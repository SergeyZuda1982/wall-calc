# wall-calc — конспект проекта (актуален на 90c5bb6, 08.07.2026)

**Репо:** github.com/SergeyZuda1982/wall-calc
**Сайт:** https://sergeyzuda1982.github.io/wall-calc/
**Стек:** React + TypeScript + Vite + react-konva + Zustand + Supabase + vitest + pdfjs-dist + **three.js/@react-three/fiber/@react-three/drei** (НОВОЕ с 03.07.2026 — см. ниже, это устарело в конспектах других чатов)
**Хостинг:** GitHub Pages (автодеплой через Actions при git push в `main`)
**GitHub token:** выдаётся пользователем в чат при необходимости пуша (~30 дней)

⚠️ **ОБЯЗАТЕЛЬНОЕ ПРАВИЛО:** перед любой правкой — `git fetch origin && git reset --hard origin/main`.
Не доверяй конспектам из старых чатов на слово — сверяй с `git log -1` и реальными файлами.
**Подтверждено на практике не раз:** конспекты параллельных чатов регулярно
отстают друг от друга на целые фичи (например, конспект от сессии
"дуги/раскрой" за 03.07.2026 всё ещё писал "в проекте нет ни одной
3D-библиотеки" — а она уже была добавлена в ДРУГОМ параллельном чате
в тот же день). Всегда сверяйся с кодом, а не только с последним
конспектом, который тебе показали.

⚠️ **ПРАВИЛО ВЕТОК:** пользователь работает с несколькими параллельными
чатами Claude (лимиты на сообщения). Работа **не идёт напрямую в main**.
1. Сначала `TASKS.md` (корень репо) — нет ли уже похожей задачи в работе.
2. Новая ветка на каждую задачу, помечаешь её в TASKS.md как "в работе".
3. `npm test && npm run build` без ошибок — обязательно перед пушем.
4. Перед мержем своей ветки — `git fetch origin && git reset --hard origin/main`
   (в main может прилететь что-то от ДРУГОГО параллельного чата — бывало
   на практике несколько раз, включая настоящий merge-конфликт 04.07.2026,
   см. ниже).
5. После мержа — `npm test && npm run build` ЕЩЁ РАЗ, уже на объединённом
   коде. "У меня было чисто" ≠ "у нас вместе чисто".
6. После пуша в main — проверить, что деплой на GitHub Pages реально
   прошёл (не просто "запушил и забыл"): вкладка Actions на GitHub,
   или `curl -H "Authorization: Bearer <токен>" -H "User-Agent: check"
   "https://api.github.com/repos/SergeyZuda1982/wall-calc/actions/runs?per_page=1"`
   — смотреть `"conclusion":"success"`. Деплой может упасть отдельно от
   кода ("Deployment failed, try again later" — было на практике,
   лечится пустым коммитом `git commit --allow-empty` + push, или Re-run
   в Actions). Задача закрыта только после успешного ДЕПЛОЯ, не пуша.
   **05.07.2026 — найдена и устранена настоящая причина**: в
   `.github/workflows/deploy.yml` не было блока `concurrency` — когда
   несколько параллельных чатов пушат в main почти одновременно, их
   деплои начинают конфликтовать друг с другом за право публикации на
   Pages, и один из них падает (не баг в коде, а гонка на уровне
   инфраструктуры). Добавлен `concurrency: {group: "pages",
   cancel-in-progress: false}` — теперь такие деплои встают в очередь,
   а не конфликтуют. Если падения возобновятся ПОСЛЕ этого — значит,
   причина уже другая, не эта гонка.
7. Перенеси задачу из "в работе" в "готово недавно" в TASKS.md.

Полный текст правил с точными командами — в конце `TASKS.md`.
**Важный урок:** `git reset --hard` стирает незакоммиченные правки
безвозвратно — коммитить стоит раньше, не копить несохранённые изменения.

---

## Сессия 07-08.07.2026: роли и участники объекта (project_members) —
## ЭТО САМАЯ СВЕЖАЯ СЕССИЯ, читать первой. Полный протокол планирования —
## отдельный файл `KONSPEKT.md`-версия v2 от 07.07.2026 был скопирован
## в чат целиком перед началом реализации (роли, права, сметы — раздел
## за разделом), в этой записи только ИТОГ реализации и что осталось.

**Статус на конец сессии:** роли и участники объекта реализованы и
работают в проде (владелец видит панель, приглашение по email работает,
проверено вживую пользователем). Сметы (`estimates`/`estimate_access`) —
только таблицы и упрощённый RLS в базе, никакого кода/UI ещё нет —
следующий большой кусок.

### Что сделано (3 ветки, все смёржены в main)

**1. `feat/project-members`** — фундамент:
- `supabase/migrations/20260707_project_members_and_estimates.sql` —
  таблица `project_members` (роли, `specialty`, `is_team_lead`/
  `team_lead_id`, статус `invited`/`active`) с триггером
  `project_members_validate()` на все проверки, которые не выразить через
  `check` (is_team_lead только у subcontractor, team_lead_id в том же
  project_id, не больше одного owner). Плюс перенос текущих владельцев
  `projects.user_id` в `project_members` (разовый insert). Плюс таблицы
  `estimates`/`estimate_access`/`personal_rate_calc` с самой первой, ещё
  багованной версией RLS (см. фикс ниже).
- `src/core/permissions.ts` — единый "проверятель прав" по матрице ролей
  (раздел 1 конспекта планирования): `can(role, action)`,
  `canManageMember(actor, target)` (прораб субподрядчика видит только
  свою команду), `findMember()`. Весь фронтенд должен спрашивать права
  ТОЛЬКО через эту функцию, не разбрасывать `if role === '...'` по
  компонентам.
- `StepProgress` (`types/index.ts`, `core/workProgress.ts`) — добавлено
  поле `userId` (кто подтвердил/отклонил шаг). `confirmStep`/`rejectStep`
  приняли необязательный `userId` БЕЗ изменения порядка существующих
  позиционных аргументов (userId добавлен в конец), старые вызовы не
  сломались.

**2. `feat/project-members-ui`** — UI и приглашение:
- `src/lib/projectMembers.ts` — CRUD-обёртки над Supabase: `fetchMembers`,
  `inviteMember` (Вариант А из раздела 4г — без email/SMS-уведомлений:
  если email уже зарегистрирован — сразу `active`, если нет — висит
  `invited` до первого входа), `changeRole`, `removeMember`,
  `transferOwnership`, `activateInvitesForUser` (вызывается при входе —
  подхватывает invited-записи по email).
- `src/store/useProjectMembersStore.ts` — zustand-обёртка.
- `src/components/ProjectMembersPanel.tsx` — модальная панель: список
  участников, форма приглашения, смена роли, удаление, отдельная кнопка
  "Сделать владельцем" (передача прав, раздел 4в) с подтверждением. Все
  кнопки гейтятся через `permissions.ts`.
- `App.tsx`: кнопка "👥 Участники" рядом с "📁 Объекты" — видна только
  когда активный объект облачный (`isCloudActive && user`).
- **Багфикс попутно:** `createProject` (`useProjectsStore.ts`) и
  `migrateLocalProjectsToCloud` (`projectCloud.ts`) раньше НЕ создавали
  owner-запись в `project_members` при создании нового объекта —
  добавлено. Без этого у любого объекта, созданного ПОСЛЕ миграции
  07.07.2026, панель участников была бы пустой навсегда.

**3. `fix/project-members-rls-recursion`** — критичный баг, найден
пользователем вживую сразу после первого деплоя:
- Политики `project_members_select`/`estimates_select` делали
  `select ... from project_members` (или `estimates` через join)
  **внутри самой политики для той же таблицы** — Postgres ловит это как
  `infinite recursion detected in policy`, запрос падает целиком.
  Проявилось как "Не удалось загрузить участников" в панели, хотя данные
  в базе были нормальные.
- Фикс: `supabase/migrations/20260708_fix_project_members_rls_recursion.sql`
  — функции `is_project_member(project_id)` и
  `has_project_role(project_id, roles[])` с `security definer` (читают
  таблицу в обход RLS вызывающего), политики переписаны через них.
- **Отдельно найдено и починено руками в SQL Editor** (не в
  git-миграции, разовый ad-hoc фикс от пользователя): два реальных
  объекта пользователя оказались вообще без owner-записи (созданы в окне
  между первой миграцией и багфиксом #2 выше) — довставлены тем же
  insert-скриптом, что и в первой миграции.
- **4-я мелкая правка (`fix/members-panel-show-email`):** у владельца
  (не прошёл инвайт-флоу) `invited_email` пуст — панель показывала голый
  Supabase `user_id` (UUID) вместо email. Прокинут `currentUserEmail`
  проп из `user.email` (сессия) в `ProjectMembersPanel`, используется
  для своей же строки в списке, когда `invited_email` нет.

### Открытый вопрос, поднятый пользователем: "ник" вместо email/UUID

Пользователь спросил, не стоит ли показывать никнейм вместо email в
списке участников. Договорились: быстрый фикс (email вместо UUID)
сделан сейчас, полноценный профиль с `display_name` — ОТДЕЛЬНАЯ будущая
фича (нужна новая таблица `user_profiles` или поле с ником, её пока нет
вообще нигде в схеме). Когда до неё дойдёт очередь — пригодится сразу в
двух местах: панель участников и `StepProgress.userId` (сейчас там сырой
auth `user_id`, ник встанет без переделки типа, поле уже `string`).

### Следующий шаг (не начат)

**Доступ к сметам через `estimate_access`** — раздел 3 конспекта
планирования (5 видов смет: `customer_full`, `materials_priced`,
`labor_priced`, `materials_qty`, `calculator_auto` + отдельная приватная
`personal_rate_calc`). Сейчас в базе только таблицы и наивный RLS
("видят все участники объекта") — нужны:
1. Функция "какие типы смет видит роль X по умолчанию" — должна жить в
   ОДНОМ месте и дублироваться на фронтенд (`permissions.ts` или новый
   `estimateAccess.ts`) и в RLS-политики (аналогично
   `is_project_member`/`has_project_role` из фикса рекурсии выше — но
   уже с учётом типа сметы, не только роли).
2. UI создания/загрузки сметы каждого типа.
3. UI выдачи персонального доступа (`estimate_access` insert) — ручные
   исключения поверх авто-правил.
4. `personal_rate_calc` — отдельный простой калькулятор ЗП, приватный,
   без `estimate_access` вообще.

После сметы — по долгосрочному плану инвентарь и микро-мессенджер
(раздел 6 конспекта планирования, оба сознательно отложены).

⚠️ **Урок этой сессии на будущее:** при написании RLS-политики ВСЕГДА
проверять, не ссылается ли она через `EXISTS`/`JOIN` сама на себя (прямо
или через ту же таблицу) — Postgres не даёт красивой ошибки на этапе
создания политики, падает только на реальном SELECT, и получатель видит
просто "не удалось загрузить данные" без деталей причины. Использовать
`security definer`-функцию для любой self-referential проверки типа
"я участник этой же таблицы" с первого раза, не постфактум.

```bash
git fetch origin && git reset --hard origin/main
npm install
npm run build   # tsc -b && vite build, без ошибок
npm test        # 676 тестов, vitest
git push        # нужен свежий токен в remote URL (~30 дней жизни)
```

---



## Сессия 08.07.2026: толщина 125мм для блочных материалов (перегородка
## в квартире заказчика) — читать после сессии про роли/участников выше,
## дальше идёт сессия того же дня про обводку (freeform), потом 07.07.2026
## про колонны

Пользователь взял реальный проект квартиры (прислали чертёж), стал
переносить его в программу. Наткнулся на перегородку, обозначенную на
чертеже как "бетонные блоки" толщиной **125мм** — съездить и проверить
на месте вживую не может, а зная типичную небрежность замерщиков,
предположил, что это скорее пенобетонный блок (такие блоки 125мм
реально продаются, проверено поиском). Попутно упомянул, что вообще
толщины у газо-/пеноблоков и монолитной плиты в программе фиксированные
наборы значений — не подгоняются под нестандартный случай.

**Разобрался в коде перед правкой (`src/data/constructionTaxonomy.ts`,
`getWallThicknessMm`):** толщина стены нигде не берётся из отдельной
таблицы "материал→толщина" — она парсится напрямую как число из
значения подтипа (`parseInt(subtype)`), сам подтип — просто список
готовых строк-опций в дереве таксономии (`'100'`, `'150'`, `'200'`...).
Значит добавление 125мм — **чистое расширение списка опций**, никакой
отдельной расчётной логики трогать не нужно (в отличие, например, от
профилей ПС, где толщина стены — это профиль+обшивка, а не голое число).

**Фикс (`feat/block-125mm-thickness`, смержено, `a70abb5`):**
Добавлена опция `125мм` в три места:
- `wall_existing → block` ("Блок / Газобетон") — это конкретно случай
  пользователя, существующая перегородка на чужом объекте
- `wall_new → gasblock` ("Газоблок / Газобетон") — проактивно, раз уж
  выяснилось, что набор толщин в принципе неполный, пригодится на
  будущих объектах для НОВЫХ стен из газоблока
- `wall_new → foamblock` ("Пеноблок") — аналогично, для новых стен

5 новых тестов (`constructionTaxonomy.blockThickness125.test.ts`) —
проверяют и наличие опции в дереве таксономии, и что
`getWallThicknessMm` реально возвращает `125` для всех трёх новых
комбинаций. 635/635 тестов зелёные (было 630), билд чист (тот же
известный warning про размер бандла three.js/pdf.js, не новый).

**Толщину монолитной плиты пользователь тоже упомянул как
"фиксированную"** — это НЕ разобрано и НЕ исправлено в этой сессии,
только зафиксирована жалоба; сам запрос на правку касался конкретно
125мм для блока, монолитную плиту пользователь отдельно не попросил
чинить прямо сейчас. Если в следующей сессии всплывёт — стоит
переспросить, что именно неудобно (какая конкретно толщина нужна и
где у плиты сейчас фиксированный список), а не чинить по догадке.

**Мерж:** после фикса — `git fetch && reset --hard origin/main` (main
не сдвинулся за время правки), мерж `--no-ff`, тесты+билд повторно на
объединённом коде — чисто. Пуш в main, деплой на GitHub Pages
подтверждён зелёным через Actions API (~1 минута).

⚠️ **Пока шла эта сессия и сразу после её пуша, в main прилетела ЕЩЁ
одна параллельная фича** — `feat/mep-layers-ui-connect` (UI инженерных
слоёв, вентиляция/электрика — судя по названию, это продолжение
фундамента `feat/mep-layers-foundation` из бэклога "слои коммуникаций",
см. секцию про бэклог идей ниже). Мерж этой фичи (`8a90b0a`) прошёл с
**настоящим конфликтом в `FloorPlan.tsx`** (тот же файл правили обе
сессии) — конфликт разрешён ДРУГИМ чатом, не этой сессией, здесь
подробности не разбирались. Текущий HEAD main (`84626eb`) уже включает
и эту фичу. Если в следующей сессии пользователь спросит про
вентиляцию/электрику на плане — не переоткрывай как новую задачу,
сначала сверься с `TASKS.md`/кодом, похоже уже сделано.


## настоящая причина нашлась только с третьей попытки
## (эта сессия шла тем же днём, 08.07.2026, ДО правки толщины 125мм
## выше — та секция теперь самая свежая; дальше после этой секции идёт
## 07.07.2026 про колонны и старые записи)

Пользователь прислал скриншот с телефона — обводит колонну/стену вплотную
к существующей конструкции (инструмент "обводка", `freeform`: стена/колонна
произвольной формы, см. `feat/freeform-structures` от 07.07.2026), контур
не замыкается. Дословно: "обводка стен и колонн не работает. после обводки
не происходит объединения. ну и начальную точку нужно сделать наверно
какого-нибудь карандаша или острого носика".

⚠️ **Важный урок этой сессии для любого будущего чата:** три фикса подряд
казались логичными и КАЖДЫЙ раз проходили тесты+билд+ручную проверку кода —
но каждый чинил РЕАЛЬНУЮ, но НЕ ГЛАВНУЮ причину. Только явный уточняющий
вопрос пользователю после второго фикса ("а вы мышью или пальцем?") вскрыл,
что фикс №2 (про тач) вообще не мог быть причиной жалобы. Не стесняйся
спрашивать заново, если пользователь говорит "всё ещё не работает" после
фикса, который тестами не покрыт (canvas-слой без юнит-тестов, полагаемся
на код-ревью + подтверждение пользователя) — не трать ещё одну ветку на
следующую гипотезу вслепую, если можно уточнить одним вопросом.

### Фикс №1 (`fix/freeform-close-snap-and-marker`, смержено, `bc97000`) —
### реальный, но НЕ единственный баг

Гипотеза: клик у первой точки контура проверялся на близость к ней ПОСЛЕ
`applySnap()` — а `applySnap` снапит к ближайшей ЧУЖОЙ линии/стене (обводка
почти всегда рисуется вплотную к реальной конструкции — в этом весь смысл
инструмента), уводя точку клика в сторону ДО сравнения расстояния. Фикс:
проверка близости к первой/промежуточным точкам — по СЫРОЙ позиции клика
(до `applySnap`), снап применяется только при добавлении НОВОЙ точки. Тот
же баг был и у `'pencil'` (плита) — тоже поправлен, там просто менее
заметен (контур обычно по периметру здания, дальше от чужих стен).
Заодно (по прямой просьбе пользователя про "острый носик") первая точка
контура получила значок-карандаш — тот же, что уже был у `'pencil'` —
плюс пунктирное кольцо вокруг нужной точки: показывает РЕАЛЬНУЮ зону тапа
для замыкания (видно с 3+ точек).

Пользователь проверил — **не помогло**, "не замыкается".

### Фикс №2 (`fix/touch-tap-swallowed-by-micro-jitter`, смержено, `75e76f9`) —
### тоже реальный баг, но НЕ причина именно этой жалобы

Уточнил у пользователя: видит ли он уже новый значок-карандаш и кольцо?
Ответ — да, оба появились, но тап ВНУТРИ кольца всё равно не замыкал.
Это означало, что фикс №1 работает корректно (кольцо = реальный порог
проверки), но что-то СОВСЕМ ДРУГОЕ мешает клику вообще дойти до проверки.
Заподозрил тач-специфичную проблему: `onTouchMoveNative` (нативный
DOM-обработчик панорамы одним пальцем) помечал `touchGestureRef.current =
true` при ЛЮБОМ, даже субпиксельном, движении пальца — без порога, в
отличие от мышиного драга (там есть `DRAG_THRESHOLD`). `handleStageClick`
в начале безусловно игнорирует тап, если `touchGestureRef.current ===
true` (защита от "призрачного клика после пинча/панорамы", более старый
фикс). Гипотеза: точное прицеливание пальцем в маленькую зону почти
неизбежно даёт микро-дрожание → тап тихо проглатывается ДО проверки
close/hitIdx. Добавлен порог `DRAG_THRESHOLD` (4px, тот же, что у мышиного
драга) перед тем, как считать одно-пальцевое движение настоящей
панорамой.

Реальный и полезный фикс сам по себе (для точных тапов на телефоне в ЛЮБОМ
режиме) — но пользователь уточнил: **репортил баг при работе МЫШЬЮ, а не
пальцем**. Это полностью исключало тач-теорию как причину именно этой
жалобы — фикс №2 остался в силе (не откатывали), но искать причину
пришлось дальше.

### Фикс №3 (`fix/freeform-stale-closure-close-never-fires`, смержено,
### `eb441f0`) — НАСТОЯЩАЯ первопричина

Раз дело не в геометрии клика (фикс №1) и не в тач-жестах (фикс №2, а
пользователь на ПК), проблема — в самом коде обработчика, а не в
математике внутри него. Проверил границы `useCallback`:
`handleStageClick` объявлен со списком зависимостей на ~280 строк кода
дальше, и в этот список **никогда не входили** `freeformPts`/
`freeformKind`/`freeformStructures`/`addFreeformStructure`/
`openingTargetFreeformId`/`addFreeformOpening`. У `'pencil'` (плита) его
`pencilPts`/`pencilHoleTargetId` в зависимостях БЫЛИ с самого начала —
видимо, забыли добавить свежедобавленные freeform-переменные по аналогии,
когда переносили функциональность 07.07.2026.

**Механизм:** пока `mode` (и остальные перечисленные зависимости) не
менялись, React ни разу не пересоздавал функцию — код внутри неё видел
ЗАСТЫВШЕЕ значение `freeformPts` от момента входа в режим (почти всегда —
пустой массив `[]`, захваченный при первом рендере в этом режиме).
Проверка `freeformPts.length >= 3` внутри такого устаревшего замыкания
была ПОСТОЯННО ложной — не из-за геометрии клика, а потому что сама
проверка вообще не видела реально накопленные точки. Клик всегда
проваливался в ветку "добавить новую точку" (та работает штатно —
использует функциональную форму `setState(prev => [...prev, pt])`, ей
чужое устаревшее значение не мешает) — отсюда и путаница: точки на экране
накапливались нормально (видно на скриншоте пользователя — 4 точки на
месте), а замкнуть контур не получалось никогда, вообще ни при каких
условиях: ни мышью, ни пальцем, ни рядом с чужой стеной, ни в чистом поле.

Фикс — тривиальный, добавил недостающие переменные в список зависимостей
`useCallback`. **Подтверждено пользователем — работает на телефоне.**
⚠️ **На ПК/мышью пока НЕ проверено** — у пользователя пока нет возможности
проверить на компьютере. Если следующая сессия начинается раньше этого
подтверждения — спросить прямо, работает ли замыкание обводки и мышью
тоже (по идее должно — причина не была специфична для мыши/тача, но
физическая проверка ещё не сделана).

### Итог по всем трём фиксам

Все три остаются в main, ни один не откатывался — каждый чинит свою
реальную (пусть и не главную) проблему:
1. Снап к чужой стене больше не уводит точку закрытия контура в сторону.
2. Точный тап на телефоне больше не проглатывается микро-дрожанием пальца.
3. Обводка теперь реально видит актуальные точки контура при попытке
   замкнуть его (была самая главная причина).

Плюс визуальное улучшение: первая точка контура — значок-карандаш (как у
плиты) + пунктирное кольцо зоны замыкания, для 'freeform' и 'pencil'.

621→630 тестов за сессию (рост в основном за счёт ДРУГИХ параллельных
чатов, не этой конкретной работы — canvas-слой FloorPlan.tsx традиционно
без юнит-тестов, полагаемся на код-ревью + подтверждение пользователя).
Три отдельных деплоя, все подтверждены зелёными через Actions API.

### ⚠️ За эту сессию в main параллельно прилетело ЕЩЁ несколько чужих фич
### — здесь НЕ разбирались подробно, только имена из TASKS.md

- `fix/parallel-wall-same-material-snap` — флэш-снап параллельных стен
  одного материала вплотную (T-снап раньше утягивал ось новой стены на
  грань соседней вместо флэш-примыкания).
- `feat/mep-layers-foundation` — фундамент инженерных слоёв (вентиляция/
  электрика) поверх архитектурного плана: типы `MepDiscipline`/`MepRoute`/
  `MepBackgrounds`, стор-экшены, БЕЗ UI пока (по аналогии с
  `calcDoubleFrame` → `feat/double-frame-ui-connect`).
- `feat/freeform-openings` — проёмы (дверь/окно/просто проём) прямо на
  обведённых (freeform) стенах, не только на прямых линиях.
- `fix/project-members-rls-recursion` — ветка существует в origin (видел
  в `git fetch`), не проверял, смержена или нет — сверься с `TASKS.md`/
  `git log` в начале следующей сессии.

Если в следующей сессии пользователь спросит про что-то из этого списка —
не переоткрывай как новую задачу, сначала проверь актуальный `git log`/
`TASKS.md`, скорее всего уже сделано другим чатом.

---

## Сессия 07.07.2026: колонны (ПКМ/Esc) + два реальных бага с 3D/этажами
## (читать ПОСЛЕ секции 08.07.2026 выше — дальше идут более старые записи,
## включая 06.07.2026 про двойной каркас)

Пользователь — прораб, чертит в основном с телефона, объяснения простые.
На вход этой сессии HEAD main был `82fb9f7` (только что влилась
параллельная фича "облачное хранение объектов" — вход по логину,
Supabase). За время сессии в main параллельно прилетело ЕЩЁ несколько
чужих фич — см. предупреждение в конце этой секции.

### 1. Инструмент «Колонны» (штамп) — ПКМ и Esc не работали как ожидалось

Жалоба: при штамповке колонн ПКМ не отменяет действие, а чтобы перестать
штамповать — обязательно нужно переключиться на другой инструмент.

Разобрался в коде (`FloorPlan.tsx`, режим `'stamp'`): ПКМ действительно
обрабатывала только промежуточный шаг у ПРЯМОУГОЛЬНОЙ колонны (ожидание
клика по углу поворота) — для круглой (ставится одним кликом) не делала
вообще ничего. `Esc` тоже только гасил этот промежуточный шаг, не
позволяя выйти из режима целиком.

**Фикс (`fix/column-stamp-cancel-and-exit`, смержено, `43c8c59`):**
- Добавлен `lastStampedRef` — ref на `{id, kind}` последней реально
  поставленной колонны (круглой или прямоугольной). ПКМ теперь: если
  есть промежуточный шаг — отменяет только его (как раньше); если шага
  нет — удаляет последнюю поставленную колонну через `removeRoundColumn`/
  `removeRectColumn`.
- `Esc`: если есть промежуточный шаг — отменяет только его; если шага
  нет — `switchMode('select')`, полный выход из режима (по аналогии с
  уже существующим поведением режима `'erase'`).
- Подсказка в панели шаблонов колонн обновлена под новые сочетания.
- Правка чисто в UI-обработчиках, новых тестов не потребовалось (как и
  для остального канвас-слоя в проекте — юнит-тестов там исторически нет,
  полагаемся на визуальную проверку).

### 2. РЕАЛЬНЫЙ БАГ №1 — `state.levels` (верхнеуровневое зеркало) не
### обновлялось при обычных правках плана

Пользователь отдельно сообщил: нарисовал плиту, нарисовал колонны,
открыл вкладку 3D — видна только плита, колонны не появились; нажал
«Дублировать этаж» — колонны наконец появились, причём сразу на ОБОИХ
этажах.

**Причина, найдена через чтение кода стора (`useProjectStore.ts`):**
общий helper `updateActiveFloorPlan()` (им пользуются ВСЕ 24 действия,
правящие план активного этажа — линии/комнаты/плиты/колонны/контуры/
подложка/масштаб) возвращал наружу только `{ floorPlan, projects }`.
`state.floorPlan` (то, что реально читает 2D-план, `FloorPlan.tsx`)
обновлялся исправно — баг был НЕ виден на самом плане. Но ОТДЕЛЬНОЕ
верхнеуровневое поле `state.levels` (то, что `Scene3D.tsx` читает для
показа ВСЕХ этажей разом, и `App.tsx` — для дебаунс-синхронизации с
облаком) обновлялось только на явных действиях с самими этажами
(`addLevel`/`duplicateLevel`/`renameLevel`/`setLevelElevation`/
`selectLevel`/`removeLevel` — они одни вызывают `syncActive()`).

**Фикс (`fix/levels-mirror-stale-on-edit`, смержено, `82c4353`):**
`updateActiveFloorPlan` теперь дополнительно возвращает пересчитанный
`levels` активного проекта — синхронно с `floorPlan`, без отдельного
вызова `syncActive` (тот требует `activeLevel` из уже ИЗМЕНЁННОГО
`projects`, лишний проход не нужен). Побочный эффект (тоже полезный) —
починилась и дебаунс-синхронизация с облаком, она следит за тем же полем.

⚠️ **Защитный доп-фикс той же сессии:** если `activeProjectId` не
совпал ни с одним проектом (activeProject не найден) — функция теперь
НЕ возвращает `levels` вообще (оставляет как было), а не подставляет
`[]`. Раньше `[]` в этой ситуации тоже тихо ломало бы 3D/панель этажей,
без всякой связи с обычным сценарием бага. Регресс-тест на этот
edge-case — отдельно, см. `useProjectStore.levelsSync.test.ts`.

4 новых теста. Проверено вручную (временный `git stash` фикса + прогон),
что без фикса все 4 падают.

### 3. РЕАЛЬНЫЙ БАГ №2 — облачный объект рождался БЕЗ единого этажа

Пользователь протестировал багфикс №1 (написал: нарисовал плиту — 3D
снова пусто, и у панели этажей рядом с «+ этаж» пропали остальные
кнопки, дублировать/удалить/переименовать/отметка). На первый взгляд
похоже на регресс от фикса №1 — но повторная проверка (изолированный
тест: `createProject` + `addSlab` + проверка `state.levels`) показала,
что стор работает верно. Настоящая причина — в СОВСЕМ ДРУГОМ месте:
параллельно смерженная фича облачного хранения (`createProject` в
`useProjectsStore.ts`) вставляла новую строку объекта в Supabase БЕЗ
поля `levels_data` вообще — `{ name, user_id }` и всё. Для локальных
объектов такой проблемы никогда не было, у них `emptyProject()` всегда
кладёт один стартовый `Level` с рождения.

Итог для нового облачного объекта: `fetchProjectContent().levels === []`
→ `activeLevelId` нигде не находится → `state.activeLevelId = null`,
`state.levels = []`. `state.floorPlan` (отдельное зеркало, не привязанное
к конкретному Level) при этом продолжает обновляться как обычно — вот
почему план рисовать МОЖНО, а 3D/панель этажей видят пустоту. Кнопки
дублировать/удалить/переименовать/отметка в `FloorPlan.tsx` все завязаны
на `activeLevelId &&` — отсюда и их исчезновение.

**Фикс (`fix/cloud-project-missing-level`, смержено, `71d7c41` + доп.
фикс типов `05df8f1`):**
1. `createProject` (облачный, `useProjectsStore.ts`) теперь сразу
   вставляет `levels_data: [emptyLevel('Этаж 1', 0)]` — та же схема,
   что у локального `emptyProject()`.
2. `loadActiveProjectEntry` защищает уже СУЩЕСТВУЮЩИЕ облачные объекты,
   созданные ДО этого фикса (у них в БД `levels_data` мог остаться
   `null`/`[]`) — если после загрузки этажей всё ещё 0, синтезирует
   один прямо на месте (`emptyLevel('Этаж 1', 0)`). Дальше он сам
   уедет в облако на первой же дебаунс-синхронизации (`useSupabaseSync`
   следит за `state.levels`) — пересоздавать объект вручную не нужно.

7 новых тестов (4 в `useProjectStore.levelsSync.test.ts`, включая
регресс на edge-case из бага №1; 3 в новом `useProjectsStore.test.ts`
с моком Supabase — `createProject` кладёт `levels_data` с одним этажом;
`loadActiveProjectEntry` лечит уже сломанный объект; НЕ трогает объект,
у которого этажи уже есть). Проверено, что без фикса 2 из 3 новых
тестов падают.

⚠️ **НЕ ПРОВЕРЕНО НА ЖИВОМ ОБЪЕКТЕ.** У пользователя не было сохранённых
облачных объектов на момент фикса (значит, лечение "старых сломанных"
объектов проверено только тестом с моком, не вживую). Чертить с телефона
пользователю неудобно — обещал проверить после работы. **Если начинаешь
следующую сессию раньше, чем пользователь подтвердил результат — спроси
прямо, проверил ли он уже создание нового облачного объекта (или вход в
старый, если он всё же есть) и появляется ли этаж/колонны/3D корректно.**
Если ещё не проверил — это первое, что стоит держать в уме как открытый
хвост, а не считать задачу полностью закрытой.

### ⚠️ Важное предупреждение — за эту сессию в main прилетело МНОГО
### чужих фич от параллельных чатов, здесь НЕ разобрано подробно

Пока шла эта сессия, в main вошли (см. `git log`/`TASKS.md` за
подробностями, здесь не разбиралось):
- `fix/touch-tap-after-pinch-pan` — тач-жесты на мобильном
- `fix/ghost-click-double-fire-on-empty-canvas` — двойной клик после тача
- `fix/c625-ps50-does-not-exist` — С625 на ПС50 не существует по нормам
  Кнауф, теперь всегда С626 (2 слоя)
- `feat/freeform-structures` — обводка произвольной формы (стена/колонна
  без привязки к прямым линиям), `FreeformStructure` в типах/сторе/3D/плане

Если в следующей сессии пользователь спросит про что-то из этого
списка — не удивляйся и не переоткрывай как новую задачу, сначала
проверь `git log`/`TASKS.md`, оно уже сделано другим чатом.

**Токен для пуша** был получен и использован в этой сессии (успел
устареть/понадобится новый в следующий раз — см. правило про ~30 дней
жизни в шапке файла).

---

## Что сделано в сессии 06.07.2026 (эта сессия — двойной каркас, ПС100)

⚠️ На входе в эту сессию пользователь дал конспект от 04.07.2026
(be888d4) — а origin/main был уже на b4f7a29 (05.07.2026), с целой
сессией по многоэтажному 3D-виду и фиксом бесконечного цикла зависания
вкладки, не попавшей в тот конспект. Традиция "сверяйся с кодом, не
только с конспектом" снова подтвердилась в первую же минуту сессии.

### 1. calcDoubleFrame.ts — механика расчёта двойного каркаса С115.1/.2/.3, С116

Закрыт пункт 4 из давнего плана (пункт 2, таксономия/дерево материалов,
был уже сделан в одной из прошлых параллельных сессий — выяснилось из
`TASKS.md`/git-лога на входе, не из конспекта пользователя).

**Архитектурная основа** (подтверждена пользователем ранее): два
независимых ряда стоек, не один каркас с общим `layers`. Стойки в обоих
рядах строго параллельны, профиль одинаковый с двух сторон — значит оба
ряда используют ОДНУ и ту же сетку позиций стоек (`buildPositions`
вызывается один раз, не дважды).

Каждый ряд считается как отдельная одинарная стена — **переиспользован
весь существующий `calcResults` целиком** (PN/PS раскрой, крепёж,
уплотнительная лента), но с обшивкой только с внешней стороны. Для этого
`calcResults.ts` получил новый параметр `sides: 1 | 2 = 2` (дефолт
сохраняет старое поведение — обратная совместимость, все существующие
вызовы не сломались), пробрасывается в формулу площади ГКЛ и в
`calcScrews`.

Различия между подтипами — только в обшивке/зазоре, каркас всегда общий:
- **С115.1**: 2+2 слоя, зазор пустой
- **С115.2**: 2+2 слоя + лист-разделитель посередине зазора (площадь —
  отдельное поле `separatorAreaM2`)
- **С115.3**: 2+3 слоя (асимметрично) — см. упрощение ниже
- **С116**: 2+2 слоя, как С115.1, но зазор увеличен под коммуникации
  (влияет только на `thicknessMm` через уже готовую `getDoubleFrameThicknessMm()`)

**Штучные отрезки ленты** между стойками (L=200мм) — там, где нет
листа-разделителя (С115.1/С115.3/С116) — подключена уже готовая с
прошлой сессии `calcDoubleFrameTapeStrips()`. Суммарная погонная лента
(`sealingTapeLm`) — просто сумма `sealingTapeLm` обоих независимых рядов
(каждый уже посчитан внутри своего `calcResults`).

⚠️ **Известное упрощение (С115.3):** `CalcResult`/`ScrewResult` по всему
проекту поддерживают максимум 2 слоя обшивки на сторону (`layer1`/`layer2`
— общий паттерн). Третий слой стороны B считается ОТДЕЛЬНО и упрощённо:
только площадь (`extraLayerAreaM2`) + тип и количество самореза TN/MN/XTN
по высоте стоек (`extraLayerScrews`), БЕЗ интеграции в общий `cutList`/
раскрой листов третьего слоя. LN11 (клопы на нахлёстах) не задваиваются —
уже посчитаны основным вызовом `calcResults` для стороны B (геометрия
стойки та же, второй вызов `calcScrews` для третьего слоя используется
только за `count25`/`code25`, остальные поля отбрасываются). Полная
интеграция потребовала бы расширения `BoardSpec`/`ScrewResult` до 3 слоёв
по всему проекту — сознательно не в этой задаче.

**Ещё не сделано:**
- Не подключено к UI — нет формы ввода `layerA1/A2/B1/B2/B3` на плане
  для линии типа С115/С116 (движок готов и протестирован, вызывать
  пока неоткуда — тот же паттерн, что был у слоя 4/5а/5б раньше).
- Угловые стыки С115↔С115 под 90° — сознательно не в этой задаче.

13 новых тестов (`calcDoubleFrame.test.ts`).

### 2. frame_ps100 в облицовке (пункт 5 из давнего плана)

Стандартный калькулятор облицовки (`LiningCalc.tsx`, `calcLining.ts`) уже
полностью поддерживал ПС100 (оверлап 1000мм, макс. высоты в
`liningMaxHeight.ts` для С625/С626) — не хватало только пути **с плана
объекта**. Правка небольшая:

- `constructionTaxonomy.ts`: `frame_ps100` в дереве `wall_lining:gkl`
  (abbr `К100`), толщина стены 115мм (профиль 100 + ~15мм обшивка, по
  аналогии с ps50→65/ps75→90)
- `planLineToLiningInput.ts`: `resolveLiningType`/`resolveLiningProfileType`
  теперь распознают `frame_ps100` (→ С625 при 1 слое, С626 при 2 —
  как и у ПС50/75)

⚠️ **Обнаруженная, но НЕ исправленная асимметрия (мелкая, на будущее):**
в стандалон-калькуляторе (`LiningCalc.tsx`) ПС50 физически не предлагается
для С625 (только для С626) — видимо, ограничение Кнауф на однослойную
облицовку с самым тонким профилем (`liningMaxHeight.ts` не содержит
`c625_ps50_*` ключей). В переводчике с плана (`planLineToLiningInput.ts`)
этого ограничения нет ни для ПС50, ни теперь для ПС100 — можно выбрать
"С625 + ПС50" на плане, это даст `getLiningMaxHeight` = 0 (не найден ключ)
и никакого предупреждения о превышении высоты. Предсуществующий пробел,
не введённый этой правкой — не трогал, чтобы не расширять небольшую
задачу. Стоит когда-нибудь синхронизировать оба пути.

7 новых тестов (`constructionTaxonomy.wallLining.test.ts` + дополнения в
`planLineToLiningInput.test.ts`).

### 3. Слияние с параллельной сессией (feat/work-stage-progress)

Пока в этом чате шла работа, в main прилетел смерженный **срез 1** другой
задачи из параллельного чата: замена `WorkStatus`/`FinishBaseStage` на
гибкую систему произвольных пользовательских этапов работ (данные +
резолвер + стартовые шаблоны, файлы `workProgress.ts`,
`workStageTemplates.ts`). Конфликт при синхронизации был только в
`TASKS.md` (оба чата дописывали список "в работе") — разрешён вручную,
сохранены оба пункта, конфликтов по коду не было.

**Важно на будущее:** старые `WorkStatus`/`FinishBaseStage` пока ЖИВЫ и
используются в `FloorPlan.tsx` — параллельная сессия сознательно не
удаляла их, пока миграция не доведена до конца. Срез 2 той задачи (UI
чек-листа на линии, переключение инспектора/`STATUS_COLORS` на новую
модель) **ещё не начат** — если в следующей сессии речь зайдёт про
статусы/этапы работ, стоит явно спросить, кто и когда продолжает эту
конкретную ветку, чтобы не задвоить работу с ещё одним параллельным чатом.

### 4. Мерж в main

`feat/double-frame-calc-engine` (2 коммита этой сессии + мерж-коммит
синхронизации с main) смержена в main обычным `--no-ff` мержем. После
мержа `npm test && npm run build` прогнаны ПОВТОРНО (как и требует
правило) — 529/529 тестов зелёные, билд чистый. Деплой на GitHub Pages
подтверждён зелёным через Actions API (~1 минута после пуша).

**Текущий HEAD main:** `79342a9`.

---

## Известные особенности / договорённости, добавленные этой сессией

13. **`calcDoubleFrame.ts` готов, но не подключён к UI.** Нужна форма на
    плане для ввода `layerA1/A2/B1/B2/(B3)` при выборе подтипа
    С115.х/С116 — по аналогии с тем, как уже сделан `step`/`layer1`/`layer2`
    для одинарного каркаса (глобальный дефолт в левой панели + per-line
    переопределение в инспекторе).

14. **С115.3, третий слой обшивки — упрощённая, не полностью
    интегрированная механика.** Если реально понадобится точный раскрой
    (не только площадь+саморезы) для третьего слоя — придётся расширять
    `BoardSpec`/`ScrewResult`/`cutList` до 3 слоёв по всему проекту,
    не только для двойного каркаса. Крупная задача, не начинать походя.

15. **`c625_ps50`/`c625_ps100` через план объекта не защищены от
    отсутствующих данных высоты** (см. раздел про ПС100 выше) — тихо
    даёт `maxHeight = 0` вместо предупреждения. Мелкая, но реальная
    дыра в UX, если кто-то реально нарисует такую комбинацию на плане.

16. ✅ **`feat/work-stage-progress`/`feat/work-progress-ui` — ЗАКРЫТО, оба
    среза в main** (см. полную секцию "Сессия 06.07.2026: статусы работ"
    ниже). Не спрашивать в начале следующей сессии "не пора ли начать
    статусы" — они уже готовы и обкатываются на реальном чек-листе.

---

## Сессия 06.07.2026: статусы работ — гибкие этапы вместо WorkStatus/FinishBaseStage (ДРУГОЙ параллельный чат, не двойной каркас выше)

Продолжение отдельного разговора (не про С115/двойной каркас — велась
параллельно, пересеклись только в `main` и в `TASKS.md`, конфликт был
только в `TASKS.md`, разрешён вручную при мерже). Пользователь описал
на реальном объекте: начертил плиту/колонны/монолит/существующие стены/
возводимые конструкции, всё уже видно в 3D — хочет добавить статусы и
**убрать из 3D то, что ещё не начато**, плюс произвольную пользовательскую
последовательность этапов на каждую поверхность (не фиксированный enum):
для существующей стены под покраску — грунтовка→штукатурка→грунтовка→
шпаклёвка→грунтовка→покраска/обои; под плитку — свой список; для
ГКЛ-перегородки — разметка→каркас→зашивка1→минвата→зашивка2→готово.
Подтверждение каждого шага — зелёная галочка либо крестик с причиной
(ждём материалов/смежников/вносим изменения/своя).

**Развилка, согласованная с пользователем перед началом:** шаблоны —
не жёсткие, у каждой линии свой независимый список (шаблон — просто
удобная заготовка, не источник истины); WorkStatus и FinishBaseStage
заменяются полностью новой системой (не сосуществуют); пункт "как это
выглядит в 3D" пользователь описал ОЧЕНЬ подробно и амбициозно (блоки
с реальной геометрией и швами, штукатурка слоем заданной толщины,
плитка с раскладкой/размером/интерактивной подрезкой мышью, ГКЛ-каркас
по-профильно с появлением слоёв) — это осознанно НЕ реализовано целиком
за одну сессию (см. "Роадмап визуала 3D по этапам" ниже), сделан только
фундамент (данные + видимость вкл/выкл), сам богатый визуал — открытая
задача на будущее, разбитая на 5 подпунктов по сложности.

### Срез 1 — данные и резолвер (коммит `06b1353`, в main)

- **`types/index.ts`**: `WorkStageTemplate`/`WorkStageTemplateStep`
  (именованный шаблон-заготовка), `WorkProgress`/`StepProgress`
  (фактический прогресс конкретной поверхности — список шагов с
  `outcome: 'pending'|'confirmed'|'rejected'`, `rejectReason`
  (`waiting_materials`/`waiting_trades`/`changes`/`other`+`rejectNote`),
  `confirmedAt`). Старые `WorkStatus`/`FinishBaseStage`/`FinishCovering`/
  `FinishState` помечены `@deprecated`, НЕ удалены (см. ниже почему).
- **`core/workProgress.ts`**: `applyTemplate`/`createWorkProgress` (свой
  список с нуля)/`saveAsTemplate`, `currentStep(Index)`, `isNotStarted`/
  `isComplete`/`isBlocked`/**`hasAnyConfirmedStep`** (специально ОТДЕЛЬНО
  от `isNotStarted` — см. пойманный edge-case ниже), `confirmStep`/
  `rejectStep`/`resetStep` (иммутабельно), `progressPercent`,
  `aggregateProgressPercent` (по СУММЕ шагов всех поверхностей, не
  среднее процентов — иначе короткий и длинный список этапов исказили
  бы вклад друг друга непропорционально).
- **`data/workStageTemplates.ts`**: 5 стартовых шаблонов по примерам
  пользователя (стена под покраску/обои, стена под плитку в санузле,
  перегородка ГКЛ, пол — стяжка+плитка, пол — наливной под ламинат) —
  редактируемые заготовки, `BUILTIN_WORK_STAGE_TEMPLATES`.
- 25 тестов на резолвер.

### Срез 2 — UI чек-листа + видимость в 3D (коммиты `791a054`, `7f7105f`, мерж `e48dfc2`, в main)

- **`core/lineProgress.ts`** — мост между `WorkProgress` конкретной линии
  и рендером: `lineProgressStatus` (`legacy|not_started|blocked|
  in_progress|complete`), `lineProgressColor`, `lineProgressSummary`,
  **`isLineBuiltForRender`**. Ключевое решение ради обратной
  совместимости: `buildProgress` НЕ задан у линии → она ведёт себя
  ТОЧНО как раньше (всегда видна) — старые планы не пропадают из 3D
  сами по себе. Скрытие срабатывает ТОЛЬКО когда пользователь ОСОЗНАННО
  завёл `buildProgress` и ни один шаг ещё не подтверждён.
- ⚠️ **Пойманный и исправленный edge-case** (см. тест
  `lineProgress.test.ts`): если отклонить (✗) самый ПЕРВЫЙ шаг, ничего
  ни разу не подтвердив, — по логике `isNotStarted()` ("все шаги
  pending") линия вышла бы из "не начато" и стала видимой, хотя
  физически ничего не построено. Поэтому видимость решает отдельная
  функция `hasAnyConfirmedStep` ("хотя бы один шаг ПОДТВЕРЖДЁН"), а не
  инверсия `isNotStarted` — это разные вопросы (статус vs видимость
  геометрии).
- **`types/index.ts`**: `PlanLine.buildProgress?` (заменяет `workStatus`),
  `finishProgressA?`/`finishProgressB?` (заменяют `finishA`/`finishB`).
- **`planTo3D.ts`**: `wallsToBoxes3D` фильтрует по `isLineBuiltForRender`
  в ОДНОЙ точке входа — `estimateCeilingMm` по-прежнему считается по
  ПОЛНОМУ списку линий (высота потолка не должна "пропадать" вместе со
  скрытой стеной).
- **`components/WorkProgressChecklist.tsx`** — переиспользуемый чек-лист:
  выбор+применение шаблона (если прогресс ещё не заведён), список шагов
  с ✓/✗/причина/сброс, "Сохранить как шаблон" (`window.prompt` на
  название — сознательно просто, не полноценный модальный UI).
- **`FloorPlan.tsx`**: блок "Этапы строительства" в инспекторе (только
  `category==='mutable'`, капитал уже стоит по определению) на
  `buildProgress`; блок "Этапы отделки" по сторонам на
  `finishProgressA/B` — заменили старые дропдауны `workStatus`/
  `FinishBaseStage`. 2D-индикатор (цветной дот на линии) и таблица
  "Конструкции на плане" переключены на `lineProgressColor`/
  `lineProgressSummary`. **Заодно найдена и убрана настоящая мёртвая
  заглушка `getStatus()` → всегда `'none'`** ("Не начата" висело у ВСЕХ
  строк таблицы с 03.07.2026, никогда не работало — см. старый конспект).
- **Дашборд**: виджет общего % выполнения над таблицей конструкций
  (полоска+число, разбивка по типу работ во `title`-тултипе) —
  `aggregateProgressPercent` по всем `buildProgress`+`finishProgressA/B`
  с непустыми `steps` (легаси-линии без прогресса не искажают процент).
- **`store/useProjectStore.ts`**: `customWorkStageTemplates?` на
  `ProjectEntry` (опционально — обратная совместимость со старыми
  сохранёнными проектами), `addCustomWorkStageTemplate`/
  `removeCustomWorkStageTemplate`.

⚠️ **Старые `WorkStatus`/`FinishBaseStage`/`FinishCovering`/`FinishState`
НЕ удалены из типов** — помечены deprecated, но `workStatus: 'existing'`
всё ещё пишется при создании колонн/при рисовании линий (см.
`defaultStatus()`/`defaultCategory()` в `FloorPlan.tsx`) — это теперь
безвредный мёртвый код (нигде не читается в рендере/инспекторе), зачистка
сознательно не в скоупе этой сессии — отдельная мелкая задача, если
кто-то возьмётся прибраться. `RoundColumn`/`RectColumn` (капитал, всегда
`'existing'`) вообще не тронуты — у капитальных конструкций своего
`buildProgress` пока нет (не запрашивалось, они и так всегда видны).

15 (workProgress) + 15 (lineProgress) + доп. тесты на `wallsToBoxes3D`
= 40 новых тестов среза 1+2 суммарно (см. точные числа в TASKS.md).
549 тестов всего на конец сессии, build+деплой зелёные.

### Роадмап визуала 3D по этапам (СОГЛАСОВАН с пользователем, НЕ начат)

Пользователь описал очень детальное видение — что должно происходить с
геометрией по мере прохождения этапов: существующий монолит/блок —
тёмно-серый → желтоватый (грунтовка) → кремовый (штукатурка, с толщиной
слоя 10/20/30мм, честно влияющей на геометрию) → белый (шпаклёвка) →
цвет по выбору (покраска, RAL) либо текстура обоев; в санузле —
штукатурка серая цементная; гидроизоляция пола — жёлтая с заходом на
стены; стяжка светлее монолита, наливной пол ещё светлее (ближе к
серо-бежевому), с толщиной каждого слоя как параметром; плитка/паркет/
ламинат — с указанием размера и направления раскладки, интерактивная
подгонка подрезки мышью; ГКЛ-каркас — сначала только профиль, потом лист
на одной стороне, потом вата, потом лист на другой — реальная,
по-этапно меняющаяся геометрия, не просто цвет.

**Согласованная разбивка на подзадачи (от простого к сложному, КАЖДАЯ —
отдельная будущая сессия/ветка, не браться на всё сразу):**

1. **Тонирование существующей коробки по этапу** (без новой геометрии) —
   существующая стена/монолит просто меняет цвет материала меша в
   `Scene3D.tsx` по текущему шагу `WorkProgress`. Самое простое, даёт
   быстрый видимый результат.
2. **ГКЛ-каркас по этапам** — геометрия реально меняется (профиль →
   +лист1 → +вата → +лист2), переиспользует размеры из `profiles.ts`/
   `constructionTaxonomy.ts`, но это уже 3-4 самостоятельных подслоя
   геометрии вместо одного бокса.
3. **Стяжка/наливной пол/гидроизоляция** — близко по сложности к (1)
   (тонирование + честная толщина слоя), но потребует расширить `Slab`
   (сейчас один контур без деления на подслои пола).
4. **Плитка с реальной раскладкой/размером/интерактивной подрезкой
   мышью** — САМОЕ сложное и трудозатратное: генерация UV/паттерна по
   размеру плитки, направление раскладки, драг для подгонки шва под
   угол. Не смешивать с остальным, отдельная крупная задача ПОСЛЕ того,
   как всё остальное обкатано на объекте.
5. **Кладка со швами и реальной геометрией кирпича/блока** (разбежка,
   видимые швы) — тоже отдельно, ближе по сложности к (4): процедурная
   генерация мелких мешей вместо одного бокса.

**Рекомендация, озвученная пользователю и принятая:** начинать с
фундамента (сделан этой сессией) + пункта 1 (тонирование) — уже закроет
исходную боль ("убрать до начала работ") и даст видимый прогресс в 3D.
Пункты 2-5 — по очереди, каждый обкатывается на реальном объекте перед
следующим.

**Мобильная адаптация** (см. "Разговор 04.07.2026" в старом конспекте —
решение "только статусы/мелкие правки на телефоне, без черчения" уже
принято, но не реализовано) — пользователь напомнил про неё мимоходом
в этой сессии ("мобильная версия требует адаптации"), в работу пока не
взято. Чек-лист `WorkProgressChecklist.tsx` сознательно сделан с крупными
зонами нажатия (кнопки 26×26px) — задел на будущий мобильный экран
статусов, но полноценной responsive-адаптации самого `FloorPlan.tsx`
это не даёт, там всё ещё десктопный canvas/drag&drop.

---

## СЛЕДУЮЩАЯ ЗАДАЧА (открыто, на конец этой сессии)

1. Разобраться с жалобой на потолки ("считают не так, как хотелось бы
   видеть") — детали не уточнены, нужно расспросить в начале сессии
   прежде чем чинить.
2. Смета крепежа на UI (`calcProjectFasteners` готова уже несколько
   сессий, не выведена) — тот же приём, что уже дважды сработал
   (раскрой листов, полный расчёт профиля).
3. Подключить `calcDoubleFrame.ts` к UI плана (см. пункт 13 выше) —
   реалистичный кандидат, если продолжать линию С115/С116.
4. `c625_ps50`/`c625_ps100` maxHeight через план — мелкая, см. пункт 15.

---



⚠️ В этот же день (05.07.2026) параллельно работали и другие чаты —
их фичи см. в `TASKS.md` разделе "Готово недавно" и в соседних секциях
этого файла (укрепление L-стыка + "Обрезать/продлить", создание
шаблона колонны "с нуля", вырезы проёмов в 3D, шаг 1 стадийной отделки
поверхностей, точный расчёт П112). Здесь — только то, что сделано
именно в ЭТОЙ сессии (карандаш → колонны).

### 1. Карандаш (`pencil`) — три бага
Подробный разбор — в `TASKS.md`, раздел "Готово недавно" за 05.07.2026
(отдельной детальной секции здесь, в KONSPEKT.md, для этого багфикса
не заводили — короткая правка, не архитектурная). Коротко: (1) экран
"отлетал" во время рисования — дорендер подложки на резкость (фича
04.07) обновлял `dataUrl`, а автоцентрирование срабатывало на ЛЮБОЕ
изменение `dataUrl`, не только на реальную новую подложку — исправлено
флагом-разграничителем `bgRerenderSkipFitRef` в `FloorPlan.tsx`;
(2) ПКМ теперь отменяет только последнюю точку контура, не весь;
(3) клик по уже поставленной точке контура удаляет именно её.

### 2. Колонны — снап в ряд с Ctrl
При штамповке шаблона (круглая — единственный клик; прямоугольная —
1-й клик, центр) зажатый Ctrl прилипает центр новой колонны к X или Y
ближайшей уже поставленной — по аналогии с ⊾90° для линий, только
привязка не угла, а позиции. Причина запроса: 40-50 одинаковых колонн
на объекте, вручную в один ряд не попасть. Живое превью (штрихпунктир
до соседней колонны) видно ещё до клика. Логика — чистые функции
`snapToColumnRow`/`nearestColumnCenter` в `core/columnStamp.ts`.

### 3. Прямоугольная колонна стала самостоятельной сущностью (`RectColumn`)
Раньше штамп прямоугольного шаблона создавал 4 линии `wall_existing` +
`Room{isColumn:true}` — теперь одна запись `RectColumn` (cx/cy/widthMm/
depthMm/angleRad/spec/label), как и круглая `RoundColumn` уже была.
**Обсуждался и осознанно отклонён** более "лёгкий" вариант (оставить
геометрию как 4 линии, поменять только слой выделения/удаления) —
пользователь предпочёл полный рефакторинг сущности, хотя он дороже,
именно чтобы не переделывать потом.

- 2D: та же генерик-штриховка (не зависит от материала), что и у
  круглой колонны — повёрнутый Konva Line-полигон через
  `rectColumnCornersPx` (готовая математика, уже была в `columnStamp.ts`).
- 3D: `BoxGeometry` — `rectColumnsToBoxes3D()` в `planTo3D.ts`,
  `rotationY = -angleRad` (тот же знак, что и у `wallToBox3D` для стен).
- Свой инспектор (название/ширина/глубина/угол/материал/шаблон/удаление),
  свой клик-маркер выбора (значок `▦`, как у круглой — `⬤`).
- **⚠️ Важно для любого будущего чата:** уже расставленные РАНЕЕ
  колонны (до 05.07.2026, старого образца — Room+4 линии) НЕ мигрированы
  в новую сущность — сознательное решение, риск потери данных на
  реальном объекте посчитали выше архитектурной чистоты. Старый код
  (рендер Room с `isColumn`, инспектор Room-колонны, `saveRectColumnAsTemplate(room: Room)`)
  **не удалён и не помечен deprecated в UI** — продолжает работать для
  старых колонн параллельно с новым `RectColumn`. Если где-то нужно
  посчитать "сколько всего колонн на плане" — источники ДВА:
  `floorPlan.rectColumns` (новые) + `floorPlan.rooms.filter(r => r.isColumn)` (старые).
  ⚠️ Это же касается и параллельной фичи "создание шаблона колонны с
  нуля" (см. соседнюю секцию "Багфикс 05.07.2026: круглую колонну
  нельзя было создать 'с нуля'" — она про шаблоны, не про сами колонны,
  не конфликтует, но проверь код при пересечении задач).

453 теста на конец именно этой сессии (без учёта того, что добавили
параллельные чаты после — сверяйся с `npm test` на актуальном main,
на момент записи этой секции в main уже 462 теста).

### 4. Инструмент "Проём" — клик по стене, без захода в панель каждой линии
Сделано ПОСЛЕ того, как этот конспект уже был написан и запушен в
первый раз (пользователь спросил "как вырезать проёмы", а затем попросил
инструмент удобнее) — если читаешь это в новом чате, значит эта фича
уже на сайте, проверь `TASKS.md`/код, но она СУЩЕСТВУЕТ.

Раньше проём (дверь/окно/просто проём для прохода) можно было добавить
только через панель конкретной линии — вручную вводя `offsetMm` (отступ
от начала линии). Проблема пользователя: (1) десятки перегородок,
перебирать их одну за одной неудобно; (2) `offsetMm` в данных всегда
считается от точки (x1,y1) линии, а линия не всегда начерчена с той
стороны, откуда на подложке PDF дан размер до проёма — приходилось
пересчитывать вручную "от какого конца я вообще чертил эту стену".

**Решение:** новый режим `'opening'` (кнопка 🚪 в панели инструментов).
Слева — тип (Дверь/Окно/Проём) + ширина/высота/низ от пола, значения
запоминаются между кликами (для серии одинаковых проёмов подряд не
надо вводить их каждый раз). Клик прямо по стене на плане — проём
ставится сразу там, ЦЕНТРИРОВАННЫЙ на точке клика (не начинается от
неё). Обработчик — в `handleLinePointerDown` (тот же, что уже был у
`select`/`erase`/`trim`), новая ветка `if (mode === 'opening')`.

Чистая математика — `openingOffsetFromClick()` в `core/geometry2d.ts`:
проецирует клик на ось линии (независимо от того, с какого конца линия
начерчена — протестировано отдельным тестом на "развёрнутой" линии),
центрирует офсет на проекции, зажимает в границы стены. 7 тестов.
Старый способ (вручную через панель линии) НЕ убран — это просто
альтернативный быстрый путь, оба пишут в одну и ту же структуру данных
(`PlanLine.openings`), несовместимости нет.

⚠️ Не решено (не просили, не делал): не проверяется пересечение с уже
существующими проёмами на той же линии — можно кликом поставить два
проёма, перекрывающих друг друга; тот же риск был и у старого способа,
не новый пробел.

---

## Бэклог идей пользователя (скинул сумбурный список идей 05.07.2026,
## ночью — статусы ниже, обновляй по мере разбора с пользователем)

Пользователь прислал большой неструктурированный список идей одним
сообщением (личные заметки "накидал перед сном"). Разобранные с ним
через уточняющие вопросы и/или уже сделанные — отмечены. Остальное —
открытый бэклог, НЕ согласован по деталям реализации, нужно уточнять
у пользователя перед началом каждого пункта (как это было сделано для
колонн — там прямое "как есть" толкование задачи оказалось неверным:
"выставлять под прямым углом" на первый взгляд читалось как поворот
фигуры, по факту оказалось — снап позиции в ряд).

**Сделано:**
- ✅ Колонны по прямой линии (Ctrl) — сделано (см. выше).
- ✅ Колонны отдельными сущностями — сделано (см. выше, `RectColumn`).
- ✅ RAL-коды для покраски — вошло в шаг 1 стадийной отделки
  (`FinishCovering` с `'paint'` + RAL, параллельный чат 05.07.2026,
  конспект по ней отдельно здесь не писан — сверяйся с TASKS.md/кодом).
- ✅ "Раздевание" конструкций по слоям (голый бетон/кладка → штукатурка →
  шпаклёвка → покраска/плитка, ГКЛ с обшивкой по сторонам раздельно) —
  **шаг 1 (модель данных + инспектор) сделан параллельным чатом**
  05.07.2026, см. TASKS.md. Плитка — только отметка выбора, БЕЗ расчёта
  раскладки/угла/объёма клея-затирки (это отдельный крупный подпункт,
  см. ниже, НЕ начат). Стяжка полов с толщиной — НЕ начата (открытый
  вопрос — привязывать к `Room` или к `Slab`, обе сущности существуют
  параллельно после многоэтажности, нужен разговор с пользователем).

**Открыто, не начато (порядок — как в исходном списке, не по приоритету):**
- ⬜ Короны — отдельная сущность, не показывать снизу как 4 стены
  (перегружает нижнюю ленту слоёв). Аналогичная логика уже проверена
  на колоннах в этой сессии — можно использовать тот же подход
  (самостоятельная сущность вместо линий), но короны — не колонны,
  геометрия другая (профиль сечения вдоль периметра, не прямоугольник/
  круг), нужно отдельно продумать форму данных.
  **09.07.2026 — обсуждали подробнее, осознанно отложено пользователем
  ("думаю это пока отложим"), не начато.** Проговорено (см. чат от
  09.07): нужны путь (замкнутый контур периметра, реже — часть стен),
  профиль сечения (ширина/высота декоративного уголка), материал
  (полиуретан/гипс/дерево/пенопласт — разный вес/цена/монтаж), высота
  установки (по умолчанию верх стены, но бывает с отступом под
  многоуровневые потолки/скрытую подсветку). Главный открытый вопрос —
  как именно описывать профиль сечения (простая ширина/высота или
  привязка к каталожному номеру профиля) — решили сначала спросить
  пользователя, как он на практике выбирает/заказывает карнизы, и
  только потом проектировать данные. Не начинать код без этого
  разговора.
- ⬜ Все конструкции — в выпадающих списках по типу (сейчас часть
  списков плоские).
- ⬜ **Левая колонка → 3 вкладки: Инструменты / Конструкции / Материалы —
  идея развита 07.07.2026, добавлена концепция "сундука".** Пользователь
  провёл прямую аналогию с инвентарём в Diablo: у персонажа есть сундук
  (весь каталог целиком) и инвентарь (то немногое, что нужно ИМЕННО
  сейчас, для текущей миссии). Перенос на wall-calc: полный каталог
  материалов/конструкций (кирпич, блок, бетон, ГКЛ/ГВЛ, все профили
  ПС50/75/100 и т.д.) — это "сундук", в реальности на КОНКРЕТНОМ объекте
  используется от силы 3-5 позиций раз за разом. "Инвентарь" — короткий
  персональный список именно под текущий объект, куда пользователь сам
  кладёт нужные материалы/конструкции из сундука — дальше тыкает по
  коротким иконкам-плиткам, не пролистывая весь каталог на телефоне
  каждый раз. Три отдельные строчки-вкладки — как и предполагалось
  раньше, но теперь ясно, что это НЕ три плоских списка, а скорее три
  раздела с одинаковой механикой "сундук + инвентарь" каждый:
  - Конструкции (стена/потолок/колонна/пол/проём/ригель/короны и т.д.) —
    по сути режимы черчения, активен только один разом.
  - Материалы (черновые: кирпич/блок/бетон..., чистовые: краска/плитка/
    обои...) — то, что вешается НА уже начерченную конструкцию.
  - Инструменты (карандаш/ножницы-обрезать-продлить/рулетка/штамп
    колонн и т.д.) — отдельные режимы работы с самим холстом, не
    привязаны к конкретной конструкции/материалу.
  Визуально — иконки-плитки вместо текущих текстовых списков (под
  каждый материал нужно будет НАРИСОВАТЬ иконку — кирпич, блок, лист
  ГКЛ, сечение профиля и т.д. — это отдельная творческая работа, не
  просто код, закладывать отдельным пунктом при оценке объёма).
  **Крупная многосессионная переделка интерфейса**, начинать не походя.
  Пользователь подтвердил: "не к спеху", в бэклоге, вернуться когда
  дойдут руки — явно НЕ в ближайший год судя по объёму текущей работы.
- ⬜ **AR (дополненная реальность) — обсуждено 07.07.2026, НЕ в работе,
  пользователь сам сказал "не в ближайший год".** Повод: шеф увидел
  видео 3D-перекрытия+колонн объекта, спросил про AR. Разобрали два
  принципиально разных уровня:
  1. **"Посмотреть готовую 3D-модель через камеру телефона, поставить на
     пол"** (как мебель в приложении IKEA) — РЕАЛИСТИЧНО и не крупная
     задача: экспорт уже готовой геометрии Scene3D в `.glb`/`.usdz` +
     готовый веб-компонент `<model-viewer>` с атрибутом `ar` — открывает
     системный AR-просмотрщик (Google Scene Viewer на Android, Apple
     Quick Look на iPhone) прямо из браузера, без установки приложения.
     Телефон просто оценивает пол по камере, никакой привязки к реальным
     стенам нет — чисто "оценить объём/масштаб".
  2. **"Навести камерой на реальную комнату и увидеть чертёж поверх
     реальных стен"** (live-трекинг + привязка плана к реальной
     геометрии) — НАМНОГО сложнее, по факту отдельный проект:
     - Техническое препятствие: полноценный WebXR (`immersive-ar`)
       работает в Chrome/Android, но **отсутствует в Safari на iPhone
       принципиально** (Apple не даёт браузерам такой доступ, только
       нативным приложениям через ARKit) — проверено веб-поиском на
       момент разговора (07.07.2026), стоит перепроверить актуальность
       перед тем как браться, статус может измениться.
     - Смысловое препятствие (сложнее технического): нужно как-то
       "привязать" начерченный план к реальным стенам объекта — то есть
       пользователь должен вручную указать 2-3 реальных угла комнаты
       через камеру, чтобы совместить план с реальностью. Это отдельная
       задача проектирования UX, не решается подключением библиотеки.
     - Без платного стороннего сервиса (например, 8th Wall — не
       проверялся детально, только упомянут как пример класса решений)
       или отдельного нативного приложения (уже не веб-сайт, отдельный
       проект вне текущего стека) — на iPhone вариант 2 не работает
       вообще.
  Если когда-нибудь возьмутся — начинать разумно с варианта 1
  (реалистичный, полезный уже сейчас для оценки масштаба на объекте),
  вариант 2 — только если станет по-настоящему нужен и появится время
  оценивать бюджет на сторонний сервис или отдельное приложение.
- ⬜ Лестничные марши в вырезанных проёмах перекрытий — форма/UX не
  продуманы, нужно отдельное обсуждение с пользователем перед началом
  (по аналогии с тем, как для колонн потребовалось уточнение — не
  делать "как понял", а спросить механику явно).
- ⬜ Слои коммуникаций (электрика/сантехника/вентиляция/пожарка) —
  геометрия, реальные размеры, нормы взаимного расположения, для
  выявления конфликтов на этапе проектирования/монтажа. Крупный
  отдельный модуль, по объёму сравним с самой системой стен.
- ⬜ Отметку "изменить высоту" (потолка) — перенести из низа экрана в
  постоянно видимую зону, желательно сразу для ВСЕХ конструкций разом
  (сейчас теряется внизу, про неё не вспоминаешь).
- ⬜ Подсказки-инструкции у каждого инструмента (раскрывающийся текст).
- ⬜ Открывание дверей/окон + ширина откосов + толщина стеклопакета —
  расширение существующих полей проёмов.
- ⬜ Статус объекта: % готовности по видам работ + общий + график
  динамики во времени. Естественно ложится НА ТОП стадийной отделки
  (когда там будет что считать) — не начинать раньше неё.
- ⬜ Анимация инструментов вдоль конструкции "в работе" (шпатели,
  кия́нка+мастерок, перфоратор+молоток, шуруповёрт+нож по типу работ) —
  декоративная фича, не приоритет, оценить объём отдельно перед началом.
- ⬜ Синхронизация освещения с реальным временем суток (лампочки по
  закату/рассвету) — декоративная фича, не приоритет.
- ⬜ Экран входа с выключателем/лампочкой — отдельная самостоятельная
  задача на дизайн логин-страницы, не пересекается с остальным.

Полный оригинальный текст идей (как прислал пользователь, без
редактуры) — см. историю чата с колонками за 05.07.2026, если нужен
буквальный оригинал формулировок.

---

## СЛЕДУЮЩАЯ ЗАДАЧА (актуально на конец сессии 05.07.2026 — колонны)

**Проверь перед началом следующего чата:**
1. `TASKS.md`, раздел "В работе" — на конец этой сессии там висели
   `fix/levels-hydration-notrigger` (кнопки этажей не отображаются у
   старых проектов после hard-refresh — миграция `levels` в
   `onRehydrateStorage` мутирует state напрямую не через `setState`)
   и `feat/multilevel-3d` — сверься с актуальным TASKS.md, список
   меняется быстро, несколько чатов работают параллельно.
2. Спроси пользователя явно, какой пункт бэклога (раздел выше) брать
   следующим — он сам расставляет приоритет по ходу работы, а не по
   порядку в списке (так было и с колоннами — из всего списка идей
   выбрал именно их первыми, без объяснения почему).
3. Для пунктов с "нужно отдельно уточнить механику" (короны, лестничные
   марши, привязка стяжки к Room/Slab) — не начинай реализацию по
   первому прочтению формулировки, задай 1 уточняющий вопрос с
   вариантами, как это было сделано для колонн.

---

## Сессия 05.07.2026 (продолжение): зависание при выборе объекта/PDF — фикс был неполный

Пользователь подтвердил: зависание (нужно закрывать вкладку) при
попытке выбрать объект в "Объекты" и при загрузке страницы PDF
СОХРАНЯЛОСЬ после предыдущего P0-фикса (fix/pdf-freeze-strip-from-persist,
`stripHeavyDataForPersist` в `partialize`).

**Почему предыдущий фикс не закрыл проблему полностью:** он чистит
тяжёлый `dataUrl` подложки только для того, что уходит на диск
(`partialize` перед `JSON.stringify`). Но у пользователя в
`localStorage` уже накопились старые записи с полноразмерными
base64-подложками, сохранённые ДО того фикса. При каждой загрузке
страницы `onRehydrateStorage` читает эти старые данные КАК ЕСТЬ (просто
мутирует state, не проходя через partialize) — тяжёлые dataUrl попадали
в live-память и висели там всю сессию, пока не перезагрузишь вкладку.
Первое же действие пользователя (выбрать объект, загрузить PDF) —
первый реальный повод React перерисовать/скопировать этот раздутый
state — и давало ощущение зависания.

**Фикс:** `stripHeavyDataForPersist()` (та же функция, что уже
существовала для записи) теперь применяется и к самому `state` сразу
после гидратации, в `onRehydrateStorage`, после миграции `levels`
(порядок важен — раньше `.levels` может не существовать у легаси-
проектов). Тест на конкретно эту ситуацию — легаси-проект с тяжёлым
`dataUrl` в localStorage → после гидратации в памяти пусто, позиция/
размер/прозрачность подложки сохранены. 475 тестов.

⚠️ Настоящий, окончательный фикс по-прежнему не сделан — перенос
подложек в IndexedDB (см. TASKS.md). Сегодняшняя правка устраняет и
"будущее", и "прошлое" раздувание localStorage/live-memory тяжёлыми
dataUrl, но подложка всё ещё не переживает перезагрузку страницы —
это осознанный, уже согласованный с пользователем временный компромисс.

---



Пользователь прислал реальные фото PDF-чертежа объекта (ЖК Аврора) —
диагональная перегородка подходит к главной стене не в одну точку, а
через короткую перпендикулярную "ступеньку" (typичный приём в
чертежах для острых/тупых углов — вместо острого шпиля стену срезают
квадратно и доводят коротким прямым участком).

**Проверил на реальных числах через сам код** (не на словах): короткий
отрезок между двумя стыками (T-стык на одном конце, L-стык на другом)
под ОСТРЫМ углом может дать самопересекающуюся (перекрученную)
геометрию заливки. Конкретно нашёл: 80мм/10° — ломается, 150мм/30°
(как на фото) и 300мм/45° — нормально. Порог не одномерный (не просто
"короче X мм" и не просто "угол меньше Y°") — ломается именно
СОЧЕТАНИЕ короткой длины с острым углом.

**Причина (для будущих сессий, если снова придётся лезть в wallJoin.ts):**
митровый стык может "утянуть" угловую точку НАЗАД за уже
зафиксированную точку на другом конце той же стены — тогда верхняя и
нижняя грани стены меняются местами по ходу оси.

**Фикс в `applyL()` (wallJoin.ts):** новая функция `safeCorner()` —
после вычисления митровых точек проверяет, не нарушают ли они порядок
точек вдоль оси стены относительно уже зафиксированного другого конца.
Если нарушают — откат на исходную (не митрованную) точку конца стены:
маленький зазор в самом углу вместо перекрученной фигуры. Проверка и
откат — НЕЗАВИСИМО для каждой из двух стен и для каждой из двух граней
(Pp/Pm), поэтому в редком случае одна сторона стыка может "не
дотянуться", а другая — дотянуться нормально (это ожидаемо и safe).
Ось расширяется под заливку ТОЛЬКО если реально применился митр (не
откат) — иначе получился бы нахлёст в другом месте.

Прогнал широкий перебор (8 длин × 13 углов = 104 комбинации) — везде
чисто. Обычные (не экстремальные) стыки дают идентичный результат, что
и раньше — 9 старых тестов `wallJoin.test.ts` прошли без изменений.
Добавлено 4 новых теста именно на этот случай.

**Новый инструмент "Обрезать/продлить" (✂, `Mode: 'trim'`, FloorPlan.tsx):**
клик по линии → клик по второй линии → ближний конец первой тянется
до пересечения с бесконечным продолжением второй. Одна операция
покрывает и "обрезать" (если конец был "перелетевший"), и "продлить"
(если "недотянутый") — что физически нужно, определяется само по тому,
какой конец ближе к точке пересечения.

Геометрия — `geometry2d.ts` → `infiniteLineIntersection()` (пересечение
БЕСКОНЕЧНЫХ прямых, не отрезков — специально, смысл инструмента именно
в том, чтобы тянуть ЗА пределы исходных отрезков). 5 новых тестов.

⚠️ **Сознательно не сделано:** обрезка/продление дуги (`sagittaMm`) —
нужно пересечение с окружностью, другая геометрия, не реализовано.
Инструмент вежливо отказывает (`alert`) с понятным сообщением, если
любая из двух линий — дуга. То же для параллельных линий (нет точки
пересечения).

462 теста всего.

---



Пользователь — прораб/монтажник, не программист, работает интуитивно,
предпочитает простой язык без терминов. Держать объяснения простыми.

### 1. Ригель (rib_beam) — новая сущность на плане
Балка перекрытия между колоннами (иногда крест-накрест, иногда
параллельно), физически единое целое с плитой перекрытия сверху —
резать нельзя, коммуникации в 3D должны идти в обход (сама логика
обхода — НЕ сделана, это просто геометрический факт зафиксирован).
Высота чистового потолка под ней ограничена её "опусканием" от плиты.

- `PlanLineType.rib_beam`, `PlanLine.sectionWidthMm`/`.dropMm` (своя
  геометрия, НЕ материальная taxonomy — `TAXONOMY.rib_beam = []`)
- Всегда `category:'capital'`, `workStatus:'existing'` по умолчанию
  (как периметр/колонны)
- Рисуется как обычная линия (клик-клик), глобальные дефолты сечения/
  опускания в левой панели
- Автоматически исключён из `wallJoin`/`attachmentResolver` (оба читают
  `hasSpec = !!spec.material`, у ригеля spec не задаётся)
- Обшивка ГКЛ на ригеле — НЕ отдельная сущность, рисуется обычной
  `wall_lining` линией рядом, как уже делается для колонн

### 2. Первая 3D-сцена объекта — **впервые в проекте появилась 3D-библиотека**
Установлены `three`, `@react-three/fiber`, `@react-three/drei`.
Новая вкладка **«🧊 3D»** в `App.tsx`, статичный вид всего, что на плане.

- `src/core/planTo3D.ts` — ЧИСТАЯ математика (без импорта three.js,
  тестируется без браузера/WebGL): `wallToBox3D` (короб на линию —
  толщина/высота/поворот вокруг Y), `estimateCeilingMm` (высота потолка
  = максимум heightMm среди wall_existing, или дефолт 3000 — это ЗАМЕНА
  настоящей высоты потолка, её как отдельной величины в модели нет),
  `roomsToPolygons3D` (контуры помещений/колонн в метры),
  `slabsToPolygons3D` (см. п.5 ниже). 19 тестов в этом файле.
- `src/core/contour.ts` — `extractContourPoints` вынесена из
  `FloorPlan.tsx`, переиспользуется и в 2D, и в 3D
- `src/Scene3D.tsx` — сборка three.js-мешей (`boxGeometry` для стен/
  ригелей, `THREE.Shape`+`ExtrudeGeometry` по контуру для пола/потолка/
  колонн), `OrbitControls`
- **Сознательные упрощения v1** (не сделано, известно):
  - ~~проёмы (двери/окна) НЕ вырезаются из объёма стены — сплошной короб~~
    — СДЕЛАНО 05.07.2026, см. "Фича 05.07.2026: вырезы проёмов в 3D" ниже
  - митры/стыки стен (`wallJoin`) не учитываются в 3D — короба просто
    накладываются друг на друга по углам
  - нет анимации/фильтра по `workStatus` — показывается вся геометрия
    как есть, статусы (уже есть в данных, см. ниже) пока не влияют на 3D
  - показывается только ОДИН этаж (активный), не все разом

### 3. Режим "полёта" в 3D (WASD + мышь)
Мышь-только (`OrbitControls`) была неудобной. `FlyControls` из drei,
переключатель в углу канваса. `dragToLook` — камера поворачивается
только при зажатой мыши, WASD двигают всегда. R/F — вверх/вниз.

**Инцидент на практике:** деплой одного из коммитов упал с "Deployment
failed, try again later" (не связано с кодом — временный сбой GitHub
Pages). Вылечено пустым коммитом-триггером. Отсюда правило 6 выше.

### 4. Многоэтажность — шаг 1 (данные + минимальный переключатель)
Пользователь был прорабом и на 5-этажном офисе — решили не ограничиваться
одним этажом сразу, закладывать многоэтажность с фундамента.

- `types/index.ts`: `Level { id, name, elevationMm, floorPlan }`,
  `emptyLevel()`. `elevationMm` — отметка НИЗА этажа от условного нуля.
- `store/useProjectStore.ts` — **рефактор структуры данных**:
  `ProjectEntry.levels: Level[]` + `activeLevelId` ВМЕСТО одного
  `floorPlan`. Все существующие действия (`addPlanLine`, `updateRoom`
  и т.п.) переведены на общий helper `updateActiveFloorPlan()` — пишут
  в план АКТИВНОГО этажа. `store.floorPlan` остался плоским зеркалом
  (= план активного этажа) — поэтому `FloorPlan.tsx`/`Scene3D.tsx`
  почти не пришлось трогать, они как и раньше читают `store.floorPlan`.
  Новые действия: `addLevel`, `duplicateLevel`, `removeLevel`,
  `renameLevel`, `setLevelElevation`, `selectLevel`.
  Миграция в `onRehydrateStorage`: старые сохранённые проекты (floorPlan
  прямо на объекте, без levels) оборачиваются в один "Этаж 1" — на
  практике сохранённых объектов сейчас нет, но на будущее не лишнее.
- `FloorPlan.tsx`: панель этажей в шапке плана — переключить/добавить/
  дублировать/удалить/переименовать/поменять отметку. **Через
  `window.prompt`, без красивых форм** — сознательно, чтобы сначала
  проверить механику, эстетика — следующий шаг ("сначала карандаш,
  потом причесать интерфейс", так решил пользователь).
- **Подтверждено пользователем на живом сайте — механика работает.**

### 5. Инструмент "карандаш" — плита + вырезание проёмов
Идея пользователя: обвести периметр здания произвольным контуром
(не привязан к прямым стенам, в отличие от `Room`) → получить плиту
пола/потолка. Тем же инструментом — вырезать дырки под лестницы/шахты/
стволы коммуникаций (коммуникации, обходящие дырки — НЕ сделано, только
геометрия дырки существует). "Дублировать" плиту на другую отметку —
задел на будущее (см. пробелы ниже), сейчас плита живёт внутри одного
этажа, копировать между этажами вручную пока негде.

- `types/index.ts`: `Slab { id, outer, holes, label }`. Своего поля
  высоты у плиты НЕТ — отметка берётся с этажа (`Level.elevationMm`),
  плита = пол текущего этажа. `FloorPlan.slabs: Slab[]`.
- `store`: `addSlab`/`updateSlabOuter`/`addSlabHole`/`removeSlab`/
  `removeSlabHole`, через тот же `updateActiveFloorPlan`.
- `FloorPlan.tsx`: режим `'pencil'` — клик добавляет точку контура,
  клик рядом с первой точкой замыкает (тот же порог, что у смыкания
  цепочки стен), ПКМ/Esc отменяет текущий контур. Список плит в левой
  панели — клик по плите переключает в режим "следующий контур — дырка
  в НЕЙ". Рендер — Konva `Shape` с evenodd-заливкой (`moveTo`/`lineTo`/
  `closePath` на внешнем контуре и каждой дырке).
- `core/planTo3D.ts`: `slabsToPolygons3D()` — контур+дырки в метры.
- `Scene3D.tsx`: `HandDrawnSlabMesh` — `THREE.Shape` + `Shape.holes`
  (`THREE.Path`), рендерится как пол (y=0). Если для этажа нарисованы
  плиты карандашом — старый авто-пол из `Room`-контуров отключается
  (`skipFloor`), чтобы не задваивалось; авто-потолок из `Room` оставлен
  как единственный источник потолка (плита пока не даёт автоматический
  потолок — это будет через плиту этажа ВЫШЕ, когда 3D научится
  показывать несколько этажей разом).
- **Не удалить плиту/дырку из UI** — действия в сторе есть, кнопок нет
  (эстетика — следующий шаг).
- Смержено в main **с настоящим merge-конфликтом** — параллельный чат
  (`feat/sealing-tape`) в то же время трогал тот же массив зависимостей
  `useCallback` в `FloorPlan.tsx`. Разрешено вручную (объединены оба
  набора зависимостей). Первый реальный конфликт за всю практику
  ветвления в этом проекте — подтверждает, что правило "fetch+reset
  перед мержем" не паранойя, а необходимость.

### Обсуждённая, но НЕ реализованная идея — односторонняя прозрачность
Пользователь описал желаемое поведение плиты в 3D: пол/потолок должны
быть непрозрачными (серый бетон до отделки) со "своей" стороны (где
стоит человек) и прозрачными с обратной — чтобы при облёте здания сверху
можно было смотреть сквозь этажи вглубь (dollhouse-вид), а находясь
внутри комнаты — видеть сплошной потолок/пол. Технически это стандартный
приём three.js (материал с `side: THREE.FrontSide`, односторонний рендер
грани) — не сложно, но НЕ сделано, отложено до многоэтажного 3D.

---

## Обновление ДОЛГОСРОЧНОГО ВИДЕНИЯ (см. полный текст ниже, зафиксирован
## другим чатом 03.07.2026 — статусы/3D/совместная работа/шаблоны)

Эта сессия **фактически начала два из четырёх слоёв** параллельно, не
дожидаясь "правильного" порядка:
- **Статусы** (`WorkStatus`/`LineCategory`) — оказались уже добавлены
  ДРУГИМ параллельным чатом (найдено попутно, см. секцию ниже про
  "статусы уже начаты"). Используются этой сессией как есть (ригель
  получает `capital`/`existing` по умолчанию), но 3D их пока НЕ читает.
- **3D-вид** — начат этой сессией (см. выше), но БЕЗ реакции на статусы
  и БЕЗ совместной работы/реалтайма.

Из этого следует главный открытый вопрос на следующую сессию:
**статусы и 3D теперь оба существуют по отдельности, но не связаны друг
с другом.** Соединить их (3D фильтрует/анимирует по `workStatus`) —
самый естественный следующий крупный шаг, если пользователь готов туда
двигаться. Старый `getStatus()` (заглушка `'none'` в `FloorPlan.tsx`)
до сих пор висит мёртвым кодом рядом с рабочим `l.workStatus` — стоит
убрать при случае, путает.

---

## Известные пробелы именно по 3D/этажам/карандашу (открыто)

1. **Проёмы не вырезаны из стен в 3D** — сплошной короб, дверь/окно
   на плане есть, в 3D нет.
2. **Митры стен (`wallJoin`) не учтены в 3D** — короба перекрываются
   по углам "как есть", визуально заметно на острых углах.
3. **3D показывает только один (активный) этаж** — нет совместного
   вида всех этажей разом с учётом их отметок. Это нужно и для
   правильных потолков (плита этажа N = потолок этажа N-1).
4. **Односторонняя прозрачность пола/потолка** — обсуждена, не сделана
   (см. выше).
5. **Дублирование ПЛИТЫ на другой этаж** — пользователь хотел именно
   это ("нарисовал пол, дублировал, задал новую отметку — получил
   потолок/пол следующего этажа"), а не дублирование ЦЕЛОГО этажа
   (это уже есть, `duplicateLevel`, но копирует вообще всё, не только
   плиту). Не сделано.
6. **Удаление плиты/дырки из UI** — только через store-действия, кнопок нет.
7. **Интерфейс этажей** — `window.prompt`, не формы. Пользователь
   осознанно попросил делать в этом порядке: сначала карандаш, ПОТОМ
   причесать интерфейс — это следующая согласованная задача.
8. **Коммуникации не обходят дырки в плите** — только геометрия дырки
   существует, логики "труба видит дырку и не проходит сквозь плиту
   мимо неё" нет.
9. Бандл сильно подрос из-за three.js (~2.1MB минифицированного JS) —
   код-сплиттинг не сделан, если станет проблемой при загрузке — делать
   отдельным шагом (`vite` уже предупреждает об этом при сборке).

---

## Архитектура данных — новое за эту сессию (сверху существующего)

```typescript
// PlanLineType — добавлен rib_beam
export type PlanLineType =
  | 'wall_new' | 'wall_lining' | 'wall_existing' | 'ceiling' | 'floor'
  | 'rib_beam'   // ригель — балка перекрытия, монолит, режется нельзя

export interface PlanLine {
  // ...существующие поля (spec, category, workStatus, openings, heightMm...)
  sectionWidthMm?: number   // rib_beam: ширина сечения по плану, мм
  dropMm?: number           // rib_beam: опускание от плиты перекрытия, мм
}

// Этаж — НОВОЕ, заменяет собой одиночный floorPlan на проекте
export interface Level {
  id: string
  name: string          // "Этаж 1", "Цоколь"...
  elevationMm: number    // отметка НИЗА этажа от условного нуля объекта
  floorPlan: FloorPlan
}

// Плита ("карандаш") — НЕ привязана к стенам, в отличие от Room
export interface Slab {
  id: string
  outer: { x: number; y: number }[]      // внешний контур, px
  holes: { x: number; y: number }[][]    // вырезы (лестницы/шахты)
  label: string
}

export interface FloorPlan {
  scaleMmPerPx: number
  lines: PlanLine[]
  contours: PlanContour[]
  rooms: Room[]
  slabs: Slab[]         // НОВОЕ
  backgroundImage?: BackgroundImage | null
}
```

`ProjectEntry` (в сторе): `levels: Level[]` + `activeLevelId: string`
ВМЕСТО `floorPlan: FloorPlan` (было раньше). Top-level `store.floorPlan`
остался как зеркало плана активного этажа — большинство кода-потребителя
(`FloorPlan.tsx`, `Scene3D.tsx`) этого не заметило.

---

## Структура файлов — новое за эту сессию

```
src/
  Scene3D.tsx                — НОВОЕ: 3D-вкладка (three.js/@react-three/fiber)
  core/
    planTo3D.ts              — НОВОЕ: чистая математика плана → 3D (19 тестов)
    contour.ts               — НОВОЕ: extractContourPoints (вынесена из FloorPlan.tsx)
  store/useProjectStore.ts   — РЕФАКТОР: levels[]+activeLevelId вместо floorPlan
  types/index.ts             — + Level, Slab, rib_beam, sectionWidthMm/dropMm
  FloorPlan.tsx               — + панель этажей, + режим 'pencil', + панель ригеля
  App.tsx                    — + вкладка "🧊 3D"
```

Плюс из ДРУГИХ параллельных чатов за эти же сутки (не в подробностях
этой сессии — если понадобятся детали, смотреть `git log`/`TASKS.md`
"готово недавно", не выдумывать по названию файла):
- `attachmentResolver.ts`, `calcAttachmentFasteners.ts`, `fastenerCatalog.ts`
  — крепёж бокового примыкания
- `planLineToSurfaceInput.ts` — переводчик план → раскрой листов
- Дуги/арки на холсте ("циркуль", `drawSagittaMm`/`sagittaMm`) — гнутые
  перегородки по хорде+стреле
- Диагональный рез листа у уклона потолка/пола в `calcSheetLayout`
- `calcSealingTape.test.ts` — судя по названию, герметизирующая лента,
  подробностей в этом конспекте нет
- Что-то из `feat/max-height-board-groups` — тоже не детализировано здесь

---

## УСТАРЕЛО (оставлено для истории, не актуально с 05.07.2026) —
## СЛЕДУЮЩАЯ ЗАДАЧА, согласованная на конец сессии 04.07.2026

Порядок, который пользователь явно попросил: **сначала карандаш (плита),
потом интерфейс** — карандаш сделан и подтверждён на сайте, значит
следующий шаг — **полировка интерфейса**:
1. Интерфейс этажей — заменить `window.prompt` на нормальные формы/панель.
2. Интерфейс плит/дырок — кнопки удаления, список с понятными подписями.
3. После интерфейса — открытые пробелы по приоритету, который решит
   пользователь: дублирование плиты на другой этаж, многоэтажный 3D-вид
   с отметками, односторонняя прозрачность, проёмы/митры в 3D, либо
   переключиться на связку статусы→3D (см. "Обновление долгосрочного
   видения" выше).

Спросить пользователя в начале следующего чата, с чего конкретно начать
из этого списка.

---

## ПОЛНЫЙ ТЕКСТ ДОЛГОСРОЧНОГО ВИДЕНИЯ (зафиксировано 03.07.2026, другим чатом)

RTS-стратегия, перенесённая на реальную стройку. Холст — не чертёж,
а живая модель объекта. Статусы объекта: существующий / неактивный /
в работе / закончен / заблокирован (с причиной). 3D-вид, реагирующий
на статус в реальном времени (сейчас — начат, но НЕ реагирует на статус,
см. выше). Совместная работа в реальном времени через Supabase (не
подключено). Шаблоны однотипных квартир — не зависит от первых двух.

---

## Фича 04.07.2026: резкость подложки PDF (умный масштаб + дорендер при зуме)

Пользователь заметил, что PDF-подложка теряет чёткость линий, причём
по-разному на разных файлах (объект — более-менее нормально при
приближении, план квартиры — заметно хуже). Разобрались, почему, и
пользователь сам предложил проверить путь через SVG (векторную
чёткость на любом масштабе) — важный тупик, который стоит помнить:

**Почему SVG НЕ подошёл (проверено, не просто отговорка):**
1. У pdf.js (используемая версия `pdfjs-dist ^6.1.200`) когда-то был
   встроенный экспорт в SVG (`SVGGraphics`), но он официально помечен
   deprecated с версии 2.15 (2022) и позже полностью убран из
   библиотеки. В нашей версии этой возможности физически нет.
2. Даже если бы был — холст построен на Konva, а она в любом случае
   рисует всё через обычный растровый `<canvas>`. "Бесконечная
   резкость SVG" — это то, что умеет показывать браузер напрямую, а не
   то, что получится внутри Konva-сцены.
3. Если исходный PDF — скан/фото бумаги, а не настоящий векторный
   чертёж, SVG вообще ничего не даст — обёртка вокруг той же самой
   картинки, ни одного нового пикселя резкости.

**Что сделали вместо этого — два независимых слоя:**

**1. Умный начальный масштаб рендера (`src/core/pdfRenderScale.ts`,
новый файл, ЧИСТАЯ логика без pdf.js/DOM — сознательно вынесена
отдельно от `pdfBackground.ts`, потому что импорт `pdfjs-dist` требует
браузерное окружение (`DOMMatrix` и т.п.) и падает в обычных
Node-тестах vitest — раньше в проекте у этого модуля вообще не было
тестов, теперь чистая часть тестируется без хитростей).**

`pickRenderScale(nativeLongSidePx, opts)` — раньше `renderScale` был
одним и тем же фиксированным числом (`2`) для всех файлов. Теперь
масштаб подбирается по РЕАЛЬНОМУ размеру страницы (PDF-пункты,
`page.getViewport({scale:1})`, не зависит от того, что нарисовано) —
цель одна и та же: длинная сторона страницы после рендера ~3000px
(`DEFAULT_TARGET_LONG_SIDE_PX`) что для маленького листа А4, что для
большого А1. Маленький лист теперь получает БОЛЬШИЙ множитель — и
детализация выравнивается между файлами разного формата. Потолок
`MAX_RENDER_SCALE=8`, чтобы совсем крошечный лист не улетел в абсурд.

**2. Дорендер из PDF при сильном зуме холста (`FloorPlan.tsx`).**
Раньше картинка подложки рендерилась один раз и дальше просто
растягивалась при зуме (отсюда размытие на приближении). Теперь: если
холст показывает подложку крупнее, чем позволяет её текущее нативное
разрешение (`bgImageEl.naturalWidth/Height`, с запасом ×1.3, чтобы не
пересчитывать по мелочи) — страница перерисовывается из исходного PDF
заново под новый целевой масштаб (`targetLongSidePx`, тоже через
`pickRenderScale`, тот же потолок `MAX_TARGET_LONG_SIDE_PX=6000`).
Мировые `width/height` (позиция и размер на плане) не трогаются —
меняется только сама картинка за ними.

⚠️ **Важное ограничение, сознательное:** сами байты PDF НИКУДА не
сохраняются (ни в localStorage, ни в Supabase) — только уже готовая
картинка, как и раньше. Исходный `File` живёт только в памяти текущей
сессии (`bgSourceRef` в `FloorPlan.tsx`, обычный `useRef`). Значит:
- дорендер при зуме работает, пока не перезагрузили страницу браузера
  (после перезагрузки — просто показывается последняя сохранённая
  картинка, дальнейшая заточка недоступна, пока не перезалить тот же
  PDF ещё раз)
- это осознанный компромисс, чтобы не раздувать хранилище постоянным
  хранением тяжёлых PDF-файлов "на всякий случай"

⚠️ **Защита от путаницы между этажами (актуально из-за многоэтажности):**
`bgSourceRef` хранит не только файл, но и `lastRenderedDataUrl` — тот
dataUrl, который САМИ только что отрендерили. Перед дорендером
сверяем: точно ли текущая подложка этажа — это то, что мы рендерили
из ЭТОГО файла (`bg.dataUrl === lastRenderedDataUrl`). Если пользователь
переключился на другой этаж с другой (или вообще без) подложкой —
проверка не пройдёт, дорендер тихо не сработает, а не попытается
натянуть чужой PDF на чужую подложку.

**Debounce 500мс** на сам вызов рендера (не на каждое микро-движение
зума) — подложка сохраняется в `localStorage` целиком при каждом
`updateBackgroundImage`, а PNG может весить пару МБ — без debounce
каждый тик колеса мыши гонял бы тяжёлую запись в хранилище.

7 новых тестов на `pickRenderScale` (разные форматы дают сравнимое
итоговое разрешение, приоритет явного `renderScale`, оба потолка,
защита от деления на 0). 357 тестов всего.

---

## Фича 03.07.2026: ввод дуги напрямую по радиусу R

Прямое продолжение фичи дуг из этой же сессии. Пользователь объяснил
конкретную причину с реального объекта (фото арок из этого же чата):
несколько арок между разными колоннами (разный пролёт/хорда), но
ЗАДАЛ одну и ту же стрелу H на каждой — из-за разной хорды у них
получился разный, никак не связанный друг с другом радиус. Правильный
путь — наоборот: один и тот же R, а H на каждом пролёте своя.

**Геометрия — `geometry2d.ts` → `sagittaFromRadius(chordLen, radius, deep)`:**
обратная задача к `arcFromChordAndSagitta` (та же формула R=(L²+H²)/2H,
решённая относительно H). Важный нюанс, из-за которого добавлен параметр
`deep`: при R больше половины хорды решения ДВА — пологая дуга
(H=R−√(R²−L²), обычный архитектурный случай) и глубокая (H=R+√(R²−L²),
больше полуокружности). При R меньше половины хорды решений нет вообще
(хорда физически не влезает в окружность такого маленького радиуса) —
возвращает `null`, вызывающий код обязан это проверять, не считать
результат всегда числом. 6 новых тестов, включая round-trip (H→R→H в
обе стороны) и ключевой практический случай (один R, разные пролёты →
разные H, но одинаковый R при проверке обратно).

**UI рисования:** переключатель "Дуга по: H | R" в левой панели.
В режиме R — поле радиуса + чекбокс "глубокая дуга". Стрела H
вычисляется в момент завершения линии (`computeSagittaForNewLine` в
`FloorPlan.tsx`), когда хорда уже известна по факту клика — до этого
момента R сам по себе не определяет H (нужна ещё и хорда). Если R
меньше минимально возможного для нарисованной хорды — `window.alert`
с понятным числом (каким мог бы быть минимальный R), линия остаётся
прямой, а не рисуется что-то неверное молча.

**Инспектор:** оба поля (H и R) видны одновременно рядом, правишь
любое — второе просто отображает то, что получилось. Плюс подсказка
"мин. R для этой хорды" прямо под чекбоксом глубокой дуги, чтобы не
пришлось считать вручную, если ввёл слишком маленький R (там, в отличие
от панели рисования, просто тихо не применяет невалидный ввод — хорда
уже известна заранее, есть время увидеть подсказку до ввода, `alert`
не нужен).

347 тестов всего.

---

## Фича 03.07.2026: дуги на холсте (арки/гнутые перегородки) — "циркуль"

Пользователь прислал реальные фото объекта (арочный проём, гнутые
радиусные потолочные перегородки) и классическую формулу определения
радиуса дуги по хорде и стреле: R=(L²+H²)/2H, L — половина хорды,
H — стрела (высота подъёма дуги от прямой). Попросил уметь чертить
дуги на холсте, используя в т.ч. эту формулу для ввода.

**Развилка, которую обсудили явно:** просто калькулятор (ввёл L/H,
получил R, без связи с чертежом) vs полноценное умение чертить дуги
на холсте как отдельный тип линии. Выбрано второе — большая работа,
сознательно.

**Данные:** `PlanLine.sagittaMm?: number` — необязательное поле.
Не задано/0 — линия прямая (как раньше, для всех старых линий ничего
не меняется). Задано — линия дуга. `x1/y1/x2/y2` остаются координатами
КОНЦОВ ХОРДЫ (не меняются от того, прямая линия или дуга) — центр
и радиус окружности вычисляются на лету, не хранятся. `lengthMm` для
дуги — это длина ДУГИ, не хорды (используется дальше во всех расчётах
материала как обычно — с этим полем ничего специально готовить не
пришлось, весь стек калькуляции просто получает верное число).

**Геометрия — `geometry2d.ts` → `arcFromChordAndSagitta()`:** строит
центр/радиус/углы/направление обхода по хорде+стреле. Знак sagitta —
сторона выгиба. Работает и для "глубоких" дуг (H > L, больше
полуокружности), не только для пологих. `arcLengthFromSagitta()` —
тонкая обёртка для случаев, когда нужна только длина, без координат
(используется при создании линии и при перетаскивании её конца).
`sampleArcPoints()` — точки вдоль дуги для рендера полилинией
(Konva `Line`, не через canvas `ctx.arc` — решение сознательное, ради
консистентности со стилем остального рендера в `FloorPlan.tsx`, там
везде обычные Konva-компоненты, ни одного `Shape`/`sceneFunc`).
21 новый тест на геометрию (полуокружность и четверть окружности
проверены вручную посчитанными числами — надёжный якорь на будущее).

**Рендер (`FloorPlan.tsx`):** для дуги — отдельная, полностью
самостоятельная ветка в цикле отрисовки линий, ДО проверки `useDouble`
и ДО обращения к `wallJoins`/`computeOpeningSegments`. Заливка стены —
полигон между двумя концентрическими дугами (радиус ±half от
центральной), это ЗНАЧИТЕЛЬНО проще, чем офсет прямой линии — центр
общий, меняется только радиус. Выбор/удаление/подпись/точки-хендлы
концов хорды — переиспользованы один в один с прямой линией (то же
`startDragLine`, тот же `handleLinePointerDown`) — концы хорды это
всё те же `x1/y1/x2/y2`, ничего для этого не пришлось переделывать.

**⚠️ Важный фикс по ходу:** перетаскивание конца линии (`handleMove`,
`dr.kind==='end1'/'end2'`) раньше безусловно писало `lengthMm` как
длину ПРЯМОЙ между новой и старой точкой. Для дуги это было бы тихой
порчей данных при первом же перетаскивании конца. Поправлено — если
`l.sagittaMm` задан, пересчитывает через `arcLengthFromSagitta`.

**Инспектор:** поле "Стрела дуги H" (0 = прямая) + сразу же расчётная
подсказка "Хорда: ... · Радиус R: ..." — тот же расчёт, что и на
объекте (натянул шнур между концами проёма, замерил стрелу посередине
рулеткой, ввёл — увидел R). Работает и на уже нарисованных прямых
линиях (превращает в дугу задним числом) и наоборот (H=0 — снова прямая).

**⚠️ Сознательно НЕ сделано в этом заходе (важно для будущих сессий):**
- **wallJoin/attachmentResolver** — дуга исключена из входа
  `computeWallJoins` (`if (l.sagittaMm) return` в useMemo). Прямая
  стена, упирающаяся в дугу, рисуется без митра/примыкания — просто
  как есть. Стык дуги со стеной — самостоятельная, непростая
  геометрическая задача (не переиспользует ни офсет прямых линий, ни
  обрезку по наклонной), не начата.
- **Проёмы (PlanOpening) на дуге** — не обрабатываются, `openings`
  на дуге просто не рендерятся сейчас (UI не запрещает их добавить,
  это стоит явно проверить/поправить в следующий заход — сейчас не
  проверялось, что произойдёт при попытке).
- **T-снап к телу дуги** — `snapPoint` проецирует точку на ПРЯМОЙ
  отрезок; для дуги это даст неверный результат где-то на теле дуги.
  Снап к КОНЦАМ хорды работает нормально (это обычный endpoint-снап,
  не менялся). Снап к середине/телу дуги — не сделан.
- **Раскрой листов (`calcSheetLayout`) по кривой стене** — не
  адаптирован. `lengthMm` (длина дуги) корректно течёт в расчёт
  материала (это ПРАВИЛЬНО для количества — саморезы/профиль по
  длине), но сама раскладка листов на диаграмме нарисует ПРЯМУЮ стену
  той же длины — визуально неверно для монтажника, но по счёту листов
  скорее всего приемлемо как грубая оценка. Не обсуждалось
  специально, не в скоупе.
- **Превью во время рисования** (пока тянешь второй клик) — рисуется
  ПРЯМОЙ линией, кривизна появляется только после того, как линия уже
  создана. Мелкое упрощение ради экономии времени, не критично —
  пользователь всё равно вводит H заранее, до клика.

**На будущее (сказано пользователем прямо):** скруглённые углы/
окружности как отдельная геометрическая область — НЕ то же самое,
что дуга по хорде+стреле, других переиспользуемых кусков пока нет
(см. комментарий в шапке `geometry2d.ts`).

**⚠️ Стык с 3D, обнаруженный при слиянии этой ветки:** параллельно
(без координации с этой сессией) появилась первая 3D-сцена
(`planTo3D.ts`, `Scene3D.tsx`, three.js/@react-three/fiber — см. раздел
ниже). Она не знает про `sagittaMm` — дуга в 3D отрисуется как прямая
стена по хорде (`x1/y1/x2/y2`), кривизна потеряется. Не чинил —
не в скоупе этой сессии и вне контекста, в котором писался `planTo3D`.
Стоит явно предупредить, когда кто-то возьмётся сводить 3D и дуги.

---

## Фича 03.07.2026: настоящий диагональный рез у уклона (не только учёт в раскрое)

Продолжение предыдущего фикса. Раньше кусок, задевающий уклон, кроился
как прямоугольник по худшей (более высокой) из двух точек колонки —
раскрой был "верный по счёту материала в целом", но конкретно этот
кусок завышал расход и не показывал, где реально проходит рез.

**Новый общий модуль `src/core/geometry2d.ts`** — универсальные 2D
примитивы, НЕ привязанные к ГКЛ и вообще к материалу: `polygonArea()`
(площадь многоугольника, формула шнурков) и `clipRectBySlopedTop()`
(обрезка прямоугольника наклонной линией, Sutherland-Hodgman по одной
полуплоскости). Осознанно вынесено из `profileGeometry.ts` (тот —
про геометрию именно профиля стены) в отдельный файл общего назначения,
т.к. следующий материал с раскроем (когда дойдёт очередь) сможет
переиспользовать эти же функции без изменений.

**В `calcSheetLayout.ts`:** кусок, который реально пересекает наклонную
линию колонки (не просто "колонка наклонная", а конкретно этот кусок
задет), получает `kind: 'diagonal_cut'` и настоящий `polygon`
(треугольник/трапеция/пятиугольник) вместо прямоугольника. Площадь
такого куска в `usedAreaM2`/`wastePercent` считается по многоугольнику,
а не по `w*h` — то есть реальные потери материала у самого уклона
теперь видны в проценте отходов, а не скрыты внутри "верного по счёту"
округления.

**В `SheetLayoutCanvas.tsx`:** такой кусок рисуется настоящим
многоугольником (Konva `Line` с `closed`), а исходная заготовка
(прямоугольник, из которого он вырезан) показана пунктиром вокруг —
видно и что реально пойдёт на стену, и сколько места кусок занял на
листе/в пуле обрезков.

**Важно на будущее (сказано пользователем прямо):** этот же класс
геометрических задач планируется и для листовых материалов, которые
появятся позже (пока в раскрое только ГКЛ/ГВЛ/Сапфир/Аквамарин — они
уже все проходят через один и тот же `calcSheetLayout`, значит фикс
уже работает для всех четырёх). Также заявлена будущая потребность
в дугах/окружностях ("нужен циркуль" — скорее всего скруглённые углы
или круглые проёмы на холсте). **Это ДРУГАЯ геометрическая область** —
пересечение отрезка с линией (сделано) и пересечение с окружностью
(не сделано) считаются разными функциями. Если возьмёмся за дуги —
им место в том же `geometry2d.ts` (уже есть комментарий-заглушка
в шапке файла), но код придётся писать с нуля, ничего не переиспользуется
напрямую из уклона.

**Диагональный физический рез листа по месту всё ещё делает монтажник
руками** — программа не советует "куда конкретно приложить рулетку",
она верно считает материал и рисует, где проходит линия. Этого
достаточно для сметы и раскроя; более детальная разметка под рез —
не обсуждалось, не в скоупе.

---

## Багфикс 03.07.2026: раскрой листов ГКЛ игнорировал уклон

**Симптом:** если в калькуляторе (App.tsx/CeilingCalc или LiningCalc.tsx)
задавался уклон потолка/пола (ломаная EdgeProfile), сам расчёт материала
(площадь ГКЛ, длины направляющих) уклон учитывал корректно — а вот
раскрой листов (`calcSheetLayout` → визуальная раскладка листов на
стене, «сколько листов и где резать») уклон игнорировал полностью.

**Причина:** `calcSheetLayout` принимал одну плоскую `wallH: number` —
вызывающий код (`App.tsx`, `LiningCalc.tsx`) заранее сворачивал профиль
в `maxStudHeight()` (одну "худшую" точку) и отдавал только её. Раскрой
кроил ВСЮ стену по этой одной высоте, как будто уклона нет вообще.

**Фикс:** `calcSheetLayout` теперь принимает `ceilingProfile`/`floorProfile`
(EdgeProfile) вместо плоской `wallH`. Точки перегиба обоих профилей
добавлены в границы колонок раскроя (`columnBoundaries`, как и края
проёмов) — внутри каждой колонки высота стены теперь линейна. Высота
колонки = худшая из высот на её двух краях (не худшая по всей стене).
Диагональный рез у самого уклона остаётся ручной подгонкой монтажника —
это осознанно не решается (см. геометрию ниже), но количество листов
и расположение горизонтальных стыков теперь верно следует форме уклона.

⚠️ **API изменился** — если где-то ещё встретится вызов
`calcSheetLayout(wallL, wallH, ...)` со старой сигнатурой (плоское
число вторым аргументом) — это будет ошибка типов, нужно завернуть
плоскую высоту в `flatProfile(wallL, h)` + `flatProfile(wallL, 0)`.
Так уже сделано в `calcProjectSheetLayout.ts` (проектный раскрой с
плана — там профили пока всегда плоские, отдельный незакрытый пункт
дорожной карты, см. ниже про `planLineToSurfaceInput`).

Добавлены тесты `calcSheetLayout.test.ts` (7 шт, раньше у файла не
было тестов вообще) — включая регрессионный тест именно на уклон.

---

## ДОЛГОСРОЧНОЕ ВИДЕНИЕ ПРОЕКТА (зафиксировано 03.07.2026, НЕ в работе)

Пользователь (монтажник/прораб, не программист — держать объяснения
простыми в диалоге с ним) сформулировал развёрнутую конечную цель.
Сознательно отложено ради текущих задач (раскрой/материалы/баги),
но должно быть на виду для планирования порядка следующих слоёв.

**Идея одной фразой:** RTS-стратегия, перенесённая на реальную стройку.
Холст — не чертёж, а живая модель объекта. Каждый объект на плане
(стена, перегородка, плитка, потолок) — не линия, а сущность со
своими свойствами и статусом. Ресурсы (материалы) считаются сразу,
как только объект появился на холсте (это уже частично работает —
расчётный движок готов, просто не для всех типов объектов и не
привязан к статусам).

**Статусы объекта** (сейчас `getStatus()` — заглушка, всегда 'none'):
- существующий (стоит и было до нас)
- неактивный (запланирован, работа не началась)
- в работе
- закончен
- заблокирован — с причиной (нет материала / ждём смежников / другое).
  Причина блока — отдельное поле, не просто ярлык, пригодится для
  списка "что сейчас держит стройку".

**3D-вид, реагирующий на статус в реальном времени.** Меняешь статус
линии на "в работе" — на вкладке 3D конструкция начинает возводиться
прямо на глазах. "Существующие" конструкции в 3D уже стоят изначально.
Технически: 3D-сцена строится из тех же данных, что и холст (материал,
высота, проёмы — всё уже есть в моделе `PlanLine`), просто нужен
рендер-слой поверх них. **В проекте пока нет НИ ОДНОЙ 3D-библиотеки** —
это будет отдельный крупный кусок работы, не мелкая фича.

**Совместная работа в реальном времени.** "Все, кто участвует в работе
над проектом" видят изменения статусов друг друга сразу. База есть
(Supabase, уже используется для авторизации/хранения проектов), но
сам realtime-канал (подписка на изменения) пока не подключён.

**Шаблоны однотипных квартир.** Если фирма делает ремонт в одинаковых
квартирах — не перерисовывать план заново на каждую, а иметь шаблон,
"проштамповать" его N раз с нумерацией, и в 3D эти копии встают друг
на друга как этажи. Логически независимо от статусов/3D-движка,
может делаться параллельно или отдельной веткой позже.

**Согласованный порядок (по итогам разговора):** статусы — это
фундамент, без него не имеет смысла начинать 3D (нечего будет туда
передавать кроме голой геометрии). 3D-движок — самый затратный по
времени слой, начинать точно не сейчас. Шаблоны квартир — не
зависят от первых двух, можно рассмотреть отдельно, если появится
практическая нужда раньше.

**Что спросить в начале сессии, если разговор зайдёт про это снова:**
готовы ли наконец взяться за статусы (это открывает путь к 3D), или
всё ещё приоритет — текущий расчётный функционал.

---

---

---

## Фича 05.07.2026: все этажи разом в 3D, на своих отметках

Пользователь спросил, как работает дублирование этажа, ожидая, что плита,
нарисованная на отметке 0, при дублировании с отметкой 3600 сразу
появится над первой в 3D. Проверил — не появлялась: `Scene3D.tsx` до
этого читал только `s.floorPlan` (план АКТИВНОГО этажа), `Level.elevationMm`
нигде в 3D вообще не использовалась. Исправлено.

**`Scene3D.tsx`:** вся геометрия одного этажа (стены/помещения/плиты/
колонны — то, что раньше строилось прямо в теле `Scene3D`) вынесена в
`LevelGroup({ floorPlan, offsetY, dimmed })` — обёрнута в
`<group position={[0, offsetY, 0]}>`, `offsetY = mmToM(level.elevationMm)`.
Сам `Scene3D` берёт `levels` (все этажи проекта) и `activeLevelId` из
стора, рендерит `<LevelGroup>` на каждый этаж. Неактивные этажи —
полупрозрачные (`opacity=0.35` на всех материалах), чтобы был виден объект
целиком, но сразу понятно, какой этаж сейчас редактируется на вкладке
«План». Никакой связи между этажами специально городить не пришлось —
плита этажа выше просто оказывается там, где её поставили по отметке,
и визуально ложится на стены этажа ниже, если отметки заданы реалистично
(что и есть весь смысл задачи).

**Заодно найден и починен смежный баг** — `duplicateLevel()` в
`useProjectStore.ts`: копия этажа генерировала линиям/помещениям/контурам
НОВЫЕ id, но `Room.lineIds`/`PlanContour.lineIds` продолжали указывать на
СТАРЫЕ id линий, которых на копии уже нет — все помещения (и старые
прямоугольные колонны образца Room+4 линии) молча пропадали на
продублированном этаже. Раньше это было почти незаметно (3D показывал
только активный этаж, второй этаж просто "открывали" и не всегда
проверяли внимательно), стало сразу бросаться в глаза, когда 3D научился
показывать все этажи разом. Логика починки вынесена в чистую функцию
`duplicateFloorPlanGeometry()` (`src/core/duplicateFloorPlan.ts`) — строит
карту старый→новый id линий и прогоняет через неё `lineIds` у rooms и
contours; `slabs`/`roundColumns`/`rectColumns` (не ссылаются на линии)
просто получают свежие id, чтобы не пересекаться между этажами.

⚠️ **Не сделано (сознательно, дальше по потребности):**
- Плавающие подписи этажей в 3D (имя/отметка прямо в сцене) — сейчас
  единственный признак "какой этаж активный" — прозрачность, подписи нет
- Если один и тот же этаж продублирован несколько раз подряд без правки
  плана — все копии лягут ровно друг в друга по X/Z (это ожидаемо и
  корректно, просто на заметку — визуально может выглядеть как "один
  толстый этаж", пока отметки не разнесены)

7 новых тестов на `duplicateFloorPlanGeometry` в
`duplicateFloorPlan.test.ts` (469 тестов всего). Ручного smoke-теста
самого 3D-рендера (three.js/WebGL) в проекте по-прежнему нет — как и для
остального `Scene3D.tsx`/`planTo3D.ts`, полагаемся на типы + чистые
функции + визуальную проверку пользователем на реальном объекте.

---

## Багфикс 05.07.2026: круглую колонну нельзя было создать "с нуля"

Пользователь спросил "как мне сейчас создавать колонны" — при проверке
всплыл тупик: `RoundColumn` создаётся только через штамп шаблона
(`addRoundColumn` вызывается ровно в одном месте, в режиме `stamp`), а
шаблон круглой колонны создаётся только кнопкой "Сохранить как шаблон" в
панели уже существующей `RoundColumn`. Для первой круглой колонны на
новом объекте образца ещё нет — курица и яйцо, тупик. Прямоугольную
колонну это не касалось: её можно нарисовать вручную 4 линиями
`wall_existing` и отметить галочкой "это колонна" в панели `Room`.

**Фикс:** в панели "Шаблоны колонн" добавлены две кнопки — «+ ▦ новый» и
«+ ⬤ новый» — создают шаблон с нуля через `window.prompt` (название,
размеры), без образца на плане. Материал такого шаблона не задан —
донастраивается уже после штамповки, у самой поставленной колонны (как и
у остальных её параметров).

---

## Идея 03.07.2026: свой визуальный стиль отрисовки профиля (НЕ в работе)

Разговор начался с вопроса пользователя про фото реального стыка ПС75×50×0.6
(митровый угол на объекте, зенитный фонарь) — не баг, а вопрос "насколько
сложно сделать отрисовку стоек ближе к реальной геометрии, но со своим
узнаваемым стилем, и в 2D, и в будущем 3D". Зафиксирован план на будущее,
без даты начала:

1. **Точное сечение профиля вместо плоского прямоугольника** (низкая
   сложность, дни, не недели). Сейчас стойка на канвасе — один прямоугольник
   с линейным градиентом (имитация объёма). Реальный профиль — гнутый лист:
   спинка + 2 полки (отгиб) + 2 губки (короткий возвратный загиб на концах).
   Рисуется параметрически один раз (по размерам из `profiles.ts`), дальше
   переиспользуется. Штамповку-насечку (ромбик на плоскостях жёсткости) —
   через SVG/Konva `Pattern`, не руками на каждую стойку.
2. **Свой фирменный стиль** (та же сложность, что и пункт 1, другое
   решение). Не фотореализм, а плоская заливка + акцентная полоса-маркер
   (цвет полосы = тип профиля ПС50/75/100, толщина полосы = толщина
   металла 0.6/0.7) + тики-засечки как на линейке. Более читаемо при
   мелком зуме на телефоне на объекте, чем реализм.
3. **Настоящий 3D** — другая лига трудозатрат, отдельный крупный кусок
   работы (в проекте пока нет ни одной 3D-библиотеки, см. "ДОЛГОСРОЧНОЕ
   ВИДЕНИЕ" выше). Предложенный путь: смоделировать эталонный отрезок
   профиля один раз в Blender (бесплатный, ближе к CAD-точности, чем к
   рисованию — extrude 2D-сечения вдоль пути + материал "оцинковка"),
   экспорт в `.glb`, переиспользовать как ассет везде, где нужен 3D-вид.
   Начинать не раньше, чем появится сама 3D-сцена (статусы → 3D, см. выше).

**Рекомендованный порядок, если возьмёмся:** сначала пункт 1 (не блокирует
остальную разработку, мгновенный визуальный эффект), пункт 2 отдельным
разговором про палитру/акценты, пункт 3 — только когда дойдёт очередь
до самой 3D-сцены по долгосрочному видению, не раньше (иначе ассет
делается впустую, негде показать).

---

---

## Фича 05.07.2026: вырезы проёмов в 3D + третий тип проёма "просто проём"

Закрывает упрощение v1 из первой 3D-сессии (см. выше) — до этого стена в
3D была сплошным коробом независимо от `openings` на линии.

**Модель данных:** `PlanOpening.type` расширен `'door' | 'window' |
'opening'` (types/index.ts). Третий тип — просто проём (проход/ниша, без
двери/окна), с `offsetMm/widthMm/heightMm/sillHeightMm` — теми же полями,
что у окна (`sillHeightMm` необязателен, по умолчанию 0 = от пола). Метка
`Пр-N`, отдельная сквозная нумерация, как у `Д-N`/`О-N`. Для расчётных
модулей (`planLineToWallInput.ts`, `planLineToLiningInput.ts`,
`planLineToSurfaceInput.ts`, тип `Opening`/`OpeningType` в types/index.ts
там ДРУГОЙ, только `'door'|'window'`) — `'opening'` мапится в `'door'`
(структурно то же самое: сплошной вырез от пола, то же обрамление), сам
`OpeningType` там не трогали, чтобы не задевать `feat/finish-stages`
(параллельная задача в той же расчётной области).

**3D-геометрия — `wallToBoxesWithOpenings3D()` в `planTo3D.ts`:** без CSG
(в проекте её нет, заводить ради этого на three.js r128 не стали) — короб
стены режется на несколько коробов поменьше вдоль оси линии, теми же
принципами, что уже использует 2D-рендер плана для дверных/оконных
разрывов (`computeOpeningSegments` в `FloorPlan.tsx`, не переиспользована
напрямую — там px/канвас-геометрия, здесь метры/3D, но логика зеркальная):
сплошной сегмент до первого проёма → на каждый проём: сегмент-"подоконник"
(пол → низ проёма, если `sillHeightMm>0`) и сегмент-"перемычка" (верх
проёма → потолок, если проём не достаёт до потолка) → сегмент между
проёмами → хвост после последнего. Сам проём — просто нет короба, дырка.
`wallsToBoxes3D()` теперь `flatMap` по этой функции вместо одного короба
на линию; `Scene3D.tsx` менять не пришлось — как и раньше просто маппит
массив `WallBox3D[]` в меши, ему всё равно, сколько коробов на одну линию.

Защита от кривых данных: офсет/ширина клэмпятся к длине стены (на случай
если линию укоротили после того, как проём уже был добавлен), а два
проёма внахлёст на одной линии не досчитывают перемычку/подоконник
дважды на уже вырезанном куске (`cutStartM = max(op.startM, curM)`).

⚠️ **Известные упрощения, оставлены как есть:**
- Только прямоугольный вырез — без скосов/арок, без четверти/откоса
  проёма (просто чистый рез на всю толщину стены)
- Проёмы на дуге (`sagittaMm`) по-прежнему не поддерживаются вообще
  (ни в 2D, ни здесь) — см. открытый пункт про дуги выше
- 2D-инспектор проёма (кнопки типа, поля формы) добавляет третий тип
  визуально, но специальных иконок (дверной свинг, штриховка окна) как не
  было, так и нет ни у одного из трёх типов — на плане проём это просто
  разрыв в стене с подписью (`Д-1`/`О-1`/`Пр-1`), это уже было так
  до этой сессии, не регрессия

11 новых тестов на `wallToBoxesWithOpenings3D` в `planTo3D.test.ts`
(431 тест всего по проекту на конец сессии).

---

```bash
git fetch origin && git reset --hard origin/main
npm install
npm run build   # tsc -b && vite build, без ошибок
npm test        # 549 тестов, vitest
git push        # нужен свежий токен в remote URL (~30 дней жизни)
```
